# SPDX-License-Identifier: Elastic-2.0

[package]
name = "automonique-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
automonique-core = { path = "../automonique-core" }
# The legacy-history backfill addresses the predecessor's schema and the durable
# keys a previous backfill wrote. Those spellings are declared once, in the
# compatibility registry that is their sanctioned home, and named from here.
automonique-protocol = { path = "../automonique-protocol" }
nix.workspace = true
rusqlite.workspace = true
sha2.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
