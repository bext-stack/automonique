# SPDX-License-Identifier: Apache-2.0

[package]
name = "automonique-platform-client"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
automonique-protocol = { path = "../../../rust/crates/automonique-protocol" }

[dev-dependencies]
tempfile = "=3.24.0"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
dbg_macro = "forbid"
todo = "forbid"
unimplemented = "forbid"
