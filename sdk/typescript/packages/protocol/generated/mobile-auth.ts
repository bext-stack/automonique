// SPDX-License-Identifier: Apache-2.0

// GENERATED by automonique_protocol::codegen — do not edit by hand.
// Regenerate with: AUTOMONIQUE_PROTOCOL_REGENERATE=1 cargo test -p automonique-protocol --test codegen
//
// Source of truth: automonique_protocol::codegen::mobile_auth_module
// Origin-bound mobile discovery, scoped credential lifecycle, and actor authorization values.
//
// Rust is the wire source of truth. Hand-written SDK code may add
// ergonomics; it may not redefine anything in this file.

import {RefusalError, ValidationError, bodyArray, bodyBool, bodyIntegerOrNull, bodyString, bodyStringOrNull, bodyStrings, bodyUnsigned, bodyValue, byteLength, exactFields, exactInputFields, exactString, isWellFormedUnicode, jsonUnsigned, mapNullable, refuse, type JsonValue} from "./runtime.js";

/** Maximum independently granted mobile actions. */
export const MAX_MOBILE_ACTIONS = 4;

/** Maximum secret-free credential summaries in one operator page. */
export const MAX_MOBILE_CREDENTIAL_PAGE_SIZE = 100;

/** Maximum negotiated UTF-8 follow-up bytes. */
export const MAX_MOBILE_FOLLOW_UP_BYTES = 65536;

/** Maximum accepted mobile lifecycle response body. */
export const MAX_MOBILE_HTTP_BODY_BYTES = 65536;

/** Maximum negotiated event-page ceiling. */
export const MAX_MOBILE_PAGE_EVENTS = 512;

/** Maximum protocol versions in discovery. */
export const MAX_MOBILE_PROTOCOL_VERSIONS = 8;

/** Maximum exact session identifiers in one credential scope. */
export const MAX_MOBILE_SESSIONS = 100;

/** A mobile lifecycle document was not its exact bounded schema. */
export const MOBILE_AUTH_INVALID_BODY = "mobile_auth_invalid_body";

/** Exact media type for mobile lifecycle documents. */
export const MOBILE_AUTH_MEDIA_TYPE = "application/vnd.automonique.mobile-auth.v1+json";

/** Stable mobile authentication protocol name. */
export const MOBILE_AUTH_PROTOCOL = "automonique.mobile-auth";

/** A discovery document names another protocol. */
export const MOBILE_AUTH_PROTOCOL_MISMATCH = "mobile_auth_protocol_mismatch";

/** A mobile lifecycle document names another schema. */
export const MOBILE_AUTH_SCHEMA_MISMATCH = "mobile_auth_schema_mismatch";

/** Stable version-one mobile authentication schema. */
export const MOBILE_AUTH_SCHEMA_V1 = "automonique.mobile-auth/v1";

/** A mobile lifecycle field fell outside its value domain. */
export const MOBILE_AUTH_VALUE_INVALID = "mobile_auth_value_invalid";

/** Absolute maximum lifetime of a one-time pairing offer. */
export const MOBILE_PAIRING_TTL_MILLIS = 300000;

/** Branded identifier, at most 46 UTF-8 bytes. */
export type MobileCredentialId = string & {readonly __brand: "MobileCredentialId"};
export const MobileCredentialId_MAX_BYTES = 46;
export const MobileCredentialId_PATTERN = /^mc_[A-Za-z0-9_-]{43}$/u;
export function MobileCredentialId(value: string): MobileCredentialId {
  if (value.length === 0) throw new ValidationError("MobileCredentialId", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileCredentialId", "invalid_character");
  if (byteLength(value) > 46) throw new ValidationError("MobileCredentialId", "too_long");
  if (!MobileCredentialId_PATTERN.test(value)) throw new ValidationError("MobileCredentialId", "invalid_character");
  return value as MobileCredentialId;
}

/** Branded identifier, at most 46 UTF-8 bytes. */
export type MobilePairingId = string & {readonly __brand: "MobilePairingId"};
export const MobilePairingId_MAX_BYTES = 46;
export const MobilePairingId_PATTERN = /^pi_[A-Za-z0-9_-]{43}$/u;
export function MobilePairingId(value: string): MobilePairingId {
  if (value.length === 0) throw new ValidationError("MobilePairingId", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobilePairingId", "invalid_character");
  if (byteLength(value) > 46) throw new ValidationError("MobilePairingId", "too_long");
  if (!MobilePairingId_PATTERN.test(value)) throw new ValidationError("MobilePairingId", "invalid_character");
  return value as MobilePairingId;
}

/** Branded identifier, at most 256 UTF-8 bytes. */
export type MobileSessionId = string & {readonly __brand: "MobileSessionId"};
export const MobileSessionId_MAX_BYTES = 256;
export const MobileSessionId_PATTERN = /^[A-Za-z0-9._:-]+$/u;
export function MobileSessionId(value: string): MobileSessionId {
  if (value.length === 0) throw new ValidationError("MobileSessionId", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileSessionId", "invalid_character");
  if (byteLength(value) > 256) throw new ValidationError("MobileSessionId", "too_long");
  if (!MobileSessionId_PATTERN.test(value)) throw new ValidationError("MobileSessionId", "invalid_character");
  return value as MobileSessionId;
}

/** Bounded string, at most 46 UTF-8 bytes. */
export type MobileAccessToken = string & {readonly __brand: "MobileAccessToken"};
export const MobileAccessToken_MAX_BYTES = 46;
export const MobileAccessToken_PATTERN = /^ma_[A-Za-z0-9_-]{43}$/u;
export function MobileAccessToken(value: string): MobileAccessToken {
  if (value.length === 0) throw new ValidationError("MobileAccessToken", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileAccessToken", "invalid_character");
  if (byteLength(value) > 46) throw new ValidationError("MobileAccessToken", "too_long");
  if (!MobileAccessToken_PATTERN.test(value)) throw new ValidationError("MobileAccessToken", "invalid_character");
  return value as MobileAccessToken;
}

/** Bounded string, at most 256 UTF-8 bytes. */
export type MobileActor = string & {readonly __brand: "MobileActor"};
export const MobileActor_MAX_BYTES = 256;
export const MobileActor_PATTERN = /^[A-Za-z0-9._:-]+$/u;
export function MobileActor(value: string): MobileActor {
  if (value.length === 0) throw new ValidationError("MobileActor", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileActor", "invalid_character");
  if (byteLength(value) > 256) throw new ValidationError("MobileActor", "too_long");
  if (!MobileActor_PATTERN.test(value)) throw new ValidationError("MobileActor", "invalid_character");
  return value as MobileActor;
}

/** Bounded string, at most 2048 UTF-8 bytes. */
export type MobileCredentialInventoryEndpoint = string & {readonly __brand: "MobileCredentialInventoryEndpoint"};
export const MobileCredentialInventoryEndpoint_MAX_BYTES = 2048;
export const MobileCredentialInventoryEndpoint_PATTERN = /^https:\/\/[^?#@]+\/api\/mobile\/credentials\/list$/u;
export function MobileCredentialInventoryEndpoint(value: string): MobileCredentialInventoryEndpoint {
  if (value.length === 0) throw new ValidationError("MobileCredentialInventoryEndpoint", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileCredentialInventoryEndpoint", "invalid_character");
  if (byteLength(value) > 2048) throw new ValidationError("MobileCredentialInventoryEndpoint", "too_long");
  if (!MobileCredentialInventoryEndpoint_PATTERN.test(value)) throw new ValidationError("MobileCredentialInventoryEndpoint", "invalid_character");
  return value as MobileCredentialInventoryEndpoint;
}

/** Bounded string, at most 2048 UTF-8 bytes. */
export type MobileCredentialRevokeEndpoint = string & {readonly __brand: "MobileCredentialRevokeEndpoint"};
export const MobileCredentialRevokeEndpoint_MAX_BYTES = 2048;
export const MobileCredentialRevokeEndpoint_PATTERN = /^https:\/\/[^?#@]+\/api\/mobile\/credentials\/revoke$/u;
export function MobileCredentialRevokeEndpoint(value: string): MobileCredentialRevokeEndpoint {
  if (value.length === 0) throw new ValidationError("MobileCredentialRevokeEndpoint", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileCredentialRevokeEndpoint", "invalid_character");
  if (byteLength(value) > 2048) throw new ValidationError("MobileCredentialRevokeEndpoint", "too_long");
  if (!MobileCredentialRevokeEndpoint_PATTERN.test(value)) throw new ValidationError("MobileCredentialRevokeEndpoint", "invalid_character");
  return value as MobileCredentialRevokeEndpoint;
}

/** Bounded string, at most 64 UTF-8 bytes. */
export type MobileErrorCode = string & {readonly __brand: "MobileErrorCode"};
export const MobileErrorCode_MAX_BYTES = 64;
export const MobileErrorCode_PATTERN = /^[a-z][a-z0-9_]{0,63}$/u;
export function MobileErrorCode(value: string): MobileErrorCode {
  if (value.length === 0) throw new ValidationError("MobileErrorCode", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileErrorCode", "invalid_character");
  if (byteLength(value) > 64) throw new ValidationError("MobileErrorCode", "too_long");
  if (!MobileErrorCode_PATTERN.test(value)) throw new ValidationError("MobileErrorCode", "invalid_character");
  return value as MobileErrorCode;
}

/** Bounded string, at most 2048 UTF-8 bytes. */
export type MobileHttpsOrigin = string & {readonly __brand: "MobileHttpsOrigin"};
export const MobileHttpsOrigin_MAX_BYTES = 2048;
export const MobileHttpsOrigin_PATTERN = /^https:\/\/[^\/?#@]+$/u;
export function MobileHttpsOrigin(value: string): MobileHttpsOrigin {
  if (value.length === 0) throw new ValidationError("MobileHttpsOrigin", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileHttpsOrigin", "invalid_character");
  if (byteLength(value) > 2048) throw new ValidationError("MobileHttpsOrigin", "too_long");
  if (!MobileHttpsOrigin_PATTERN.test(value)) throw new ValidationError("MobileHttpsOrigin", "invalid_character");
  return value as MobileHttpsOrigin;
}

/** Bounded string, at most 2048 UTF-8 bytes. */
export type MobileOperatorProvisionEndpoint = string & {readonly __brand: "MobileOperatorProvisionEndpoint"};
export const MobileOperatorProvisionEndpoint_MAX_BYTES = 2048;
export const MobileOperatorProvisionEndpoint_PATTERN = /^https:\/\/[^?#@]+\/api\/mobile\/operator-provision$/u;
export function MobileOperatorProvisionEndpoint(value: string): MobileOperatorProvisionEndpoint {
  if (value.length === 0) throw new ValidationError("MobileOperatorProvisionEndpoint", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileOperatorProvisionEndpoint", "invalid_character");
  if (byteLength(value) > 2048) throw new ValidationError("MobileOperatorProvisionEndpoint", "too_long");
  if (!MobileOperatorProvisionEndpoint_PATTERN.test(value)) throw new ValidationError("MobileOperatorProvisionEndpoint", "invalid_character");
  return value as MobileOperatorProvisionEndpoint;
}

/** Bounded string, at most 2048 UTF-8 bytes. */
export type MobilePairingCreateEndpoint = string & {readonly __brand: "MobilePairingCreateEndpoint"};
export const MobilePairingCreateEndpoint_MAX_BYTES = 2048;
export const MobilePairingCreateEndpoint_PATTERN = /^https:\/\/[^?#@]+\/api\/mobile\/pairings$/u;
export function MobilePairingCreateEndpoint(value: string): MobilePairingCreateEndpoint {
  if (value.length === 0) throw new ValidationError("MobilePairingCreateEndpoint", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobilePairingCreateEndpoint", "invalid_character");
  if (byteLength(value) > 2048) throw new ValidationError("MobilePairingCreateEndpoint", "too_long");
  if (!MobilePairingCreateEndpoint_PATTERN.test(value)) throw new ValidationError("MobilePairingCreateEndpoint", "invalid_character");
  return value as MobilePairingCreateEndpoint;
}

/** Bounded string, at most 2048 UTF-8 bytes. */
export type MobilePairingExchangeEndpoint = string & {readonly __brand: "MobilePairingExchangeEndpoint"};
export const MobilePairingExchangeEndpoint_MAX_BYTES = 2048;
export const MobilePairingExchangeEndpoint_PATTERN = /^https:\/\/[^?#@]+\/api\/mobile\/pairings\/exchange$/u;
export function MobilePairingExchangeEndpoint(value: string): MobilePairingExchangeEndpoint {
  if (value.length === 0) throw new ValidationError("MobilePairingExchangeEndpoint", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobilePairingExchangeEndpoint", "invalid_character");
  if (byteLength(value) > 2048) throw new ValidationError("MobilePairingExchangeEndpoint", "too_long");
  if (!MobilePairingExchangeEndpoint_PATTERN.test(value)) throw new ValidationError("MobilePairingExchangeEndpoint", "invalid_character");
  return value as MobilePairingExchangeEndpoint;
}

/** Bounded string, at most 46 UTF-8 bytes. */
export type MobilePairingToken = string & {readonly __brand: "MobilePairingToken"};
export const MobilePairingToken_MAX_BYTES = 46;
export const MobilePairingToken_PATTERN = /^mp_[A-Za-z0-9_-]{43}$/u;
export function MobilePairingToken(value: string): MobilePairingToken {
  if (value.length === 0) throw new ValidationError("MobilePairingToken", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobilePairingToken", "invalid_character");
  if (byteLength(value) > 46) throw new ValidationError("MobilePairingToken", "too_long");
  if (!MobilePairingToken_PATTERN.test(value)) throw new ValidationError("MobilePairingToken", "invalid_character");
  return value as MobilePairingToken;
}

/** Bounded string, at most 2048 UTF-8 bytes. */
export type MobilePlatformEndpoint = string & {readonly __brand: "MobilePlatformEndpoint"};
export const MobilePlatformEndpoint_MAX_BYTES = 2048;
export const MobilePlatformEndpoint_PATTERN = /^https:\/\/[^?#@]+\/api\/platform$/u;
export function MobilePlatformEndpoint(value: string): MobilePlatformEndpoint {
  if (value.length === 0) throw new ValidationError("MobilePlatformEndpoint", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobilePlatformEndpoint", "invalid_character");
  if (byteLength(value) > 2048) throw new ValidationError("MobilePlatformEndpoint", "too_long");
  if (!MobilePlatformEndpoint_PATTERN.test(value)) throw new ValidationError("MobilePlatformEndpoint", "invalid_character");
  return value as MobilePlatformEndpoint;
}

/** Bounded string, at most 46 UTF-8 bytes. */
export type MobileRefreshToken = string & {readonly __brand: "MobileRefreshToken"};
export const MobileRefreshToken_MAX_BYTES = 46;
export const MobileRefreshToken_PATTERN = /^mr_[A-Za-z0-9_-]{43}$/u;
export function MobileRefreshToken(value: string): MobileRefreshToken {
  if (value.length === 0) throw new ValidationError("MobileRefreshToken", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileRefreshToken", "invalid_character");
  if (byteLength(value) > 46) throw new ValidationError("MobileRefreshToken", "too_long");
  if (!MobileRefreshToken_PATTERN.test(value)) throw new ValidationError("MobileRefreshToken", "invalid_character");
  return value as MobileRefreshToken;
}

/** Bounded string, at most 71 UTF-8 bytes. */
export type MobileServerIdentity = string & {readonly __brand: "MobileServerIdentity"};
export const MobileServerIdentity_MAX_BYTES = 71;
export const MobileServerIdentity_PATTERN = /^sha256:[0-9a-f]{64}$/u;
export function MobileServerIdentity(value: string): MobileServerIdentity {
  if (value.length === 0) throw new ValidationError("MobileServerIdentity", "empty");
  if (!isWellFormedUnicode(value)) throw new ValidationError("MobileServerIdentity", "invalid_character");
  if (byteLength(value) > 71) throw new ValidationError("MobileServerIdentity", "too_long");
  if (!MobileServerIdentity_PATTERN.test(value)) throw new ValidationError("MobileServerIdentity", "invalid_character");
  return value as MobileServerIdentity;
}

/** Bounded integer in [1, 100]. */
export type MobileCredentialPageSize = bigint & {readonly __brand: "MobileCredentialPageSize"};
export const MobileCredentialPageSize_MIN = 1n;
export const MobileCredentialPageSize_MAX = 100n;
export function MobileCredentialPageSize(value: bigint): MobileCredentialPageSize {
  if (typeof value !== "bigint" || value < 1n || value > 100n) throw new ValidationError("MobileCredentialPageSize", "out_of_range");
  return value as MobileCredentialPageSize;
}

/** Bounded integer in [0, 9007199254740991]. */
export type MobileEpochMillis = bigint & {readonly __brand: "MobileEpochMillis"};
export const MobileEpochMillis_MIN = 0n;
export const MobileEpochMillis_MAX = 9007199254740991n;
export function MobileEpochMillis(value: bigint): MobileEpochMillis {
  if (typeof value !== "bigint" || value < 0n || value > 9007199254740991n) throw new ValidationError("MobileEpochMillis", "out_of_range");
  return value as MobileEpochMillis;
}

/** Bounded integer in [1, 65536]. */
export type MobileFollowUpBytes = bigint & {readonly __brand: "MobileFollowUpBytes"};
export const MobileFollowUpBytes_MIN = 1n;
export const MobileFollowUpBytes_MAX = 65536n;
export function MobileFollowUpBytes(value: bigint): MobileFollowUpBytes {
  if (typeof value !== "bigint" || value < 1n || value > 65536n) throw new ValidationError("MobileFollowUpBytes", "out_of_range");
  return value as MobileFollowUpBytes;
}

/** Bounded integer in [1, 512]. */
export type MobilePageEvents = bigint & {readonly __brand: "MobilePageEvents"};
export const MobilePageEvents_MIN = 1n;
export const MobilePageEvents_MAX = 512n;
export function MobilePageEvents(value: bigint): MobilePageEvents {
  if (typeof value !== "bigint" || value < 1n || value > 512n) throw new ValidationError("MobilePageEvents", "out_of_range");
  return value as MobilePageEvents;
}

/** Bounded integer in [1, 1]. */
export type MobileProtocolVersion = bigint & {readonly __brand: "MobileProtocolVersion"};
export const MobileProtocolVersion_MIN = 1n;
export const MobileProtocolVersion_MAX = 1n;
export function MobileProtocolVersion(value: bigint): MobileProtocolVersion {
  if (typeof value !== "bigint" || value < 1n || value > 1n) throw new ValidationError("MobileProtocolVersion", "out_of_range");
  return value as MobileProtocolVersion;
}

/** Bounded integer in [1, 9007199254740991]. */
export type MobileRevision = bigint & {readonly __brand: "MobileRevision"};
export const MobileRevision_MIN = 1n;
export const MobileRevision_MAX = 9007199254740991n;
export function MobileRevision(value: bigint): MobileRevision {
  if (typeof value !== "bigint" || value < 1n || value > 9007199254740991n) throw new ValidationError("MobileRevision", "out_of_range");
  return value as MobileRevision;
}

export type MobileAction = "attach" | "decide_approval" | "follow_up" | "stop_run";
export const MobileAction_VALUES: readonly MobileAction[] = ["attach", "decide_approval", "follow_up", "stop_run"];
/** Security-sensitive: an undefined value is refused. */
export function decodeMobileAction(value: string): MobileAction {
  if (!(MobileAction_VALUES as readonly string[]).includes(value)) {
    throw new ValidationError("MobileAction", "unknown_enum_value");
  }
  return value as MobileAction;
}

/** Rotating scoped credentials and their admitted authorization. */
export interface IssuedMobileCredentials {
  readonly access_token: MobileAccessToken;
  readonly authorization: MobileAuthorization;
  readonly refresh_token: MobileRefreshToken;
}

/** The exact key set this nested body carries. */
export const IssuedMobileCredentials_FIELDS: readonly string[] = [
  "access_token",
  "authorization",
  "refresh_token",
];

export function decodeIssuedMobileCredentials(body: JsonValue): IssuedMobileCredentials {
  const fields = exactFields(body, IssuedMobileCredentials_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    access_token: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileAccessToken(bodyString(fields, "access_token", MOBILE_AUTH_INVALID_BODY))),
    authorization: decodeMobileAuthorization(bodyValue(fields, "authorization", MOBILE_AUTH_INVALID_BODY)),
    refresh_token: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileRefreshToken(bodyString(fields, "refresh_token", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Complete actor authorization admitted before Platform access. */
export interface MobileAuthorization {
  readonly actions: readonly MobileAction[];
  readonly actor: MobileActor;
  readonly authorization_revision: MobileRevision;
  readonly credential_id: MobileCredentialId;
  readonly credential_revision: MobileRevision;
  readonly expires_at_ms: MobileEpochMillis;
  readonly issued_at_ms: MobileEpochMillis;
  readonly limits: MobileLimits;
  readonly schema: typeof MOBILE_AUTH_SCHEMA_V1;
  readonly server_identity: MobileServerIdentity;
  readonly session_scope: readonly MobileSessionId[];
}

/** The exact key set this nested body carries. */
export const MobileAuthorization_FIELDS: readonly string[] = [
  "actions",
  "actor",
  "authorization_revision",
  "credential_id",
  "credential_revision",
  "expires_at_ms",
  "issued_at_ms",
  "limits",
  "schema",
  "server_identity",
  "session_scope",
];

export function decodeMobileAuthorization(body: JsonValue): MobileAuthorization {
  const fields = exactFields(body, MobileAuthorization_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    actions: bodyStrings(fields, "actions", MOBILE_AUTH_INVALID_BODY, MAX_MOBILE_ACTIONS, MOBILE_AUTH_VALUE_INVALID).map((value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => decodeMobileAction(value)),
    ),
    actor: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileActor(bodyString(fields, "actor", MOBILE_AUTH_INVALID_BODY))),
    authorization_revision: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileRevision(bodyUnsigned(fields, "authorization_revision", MOBILE_AUTH_INVALID_BODY))),
    credential_id: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialId(bodyString(fields, "credential_id", MOBILE_AUTH_INVALID_BODY))),
    credential_revision: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileRevision(bodyUnsigned(fields, "credential_revision", MOBILE_AUTH_INVALID_BODY))),
    expires_at_ms: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileEpochMillis(bodyUnsigned(fields, "expires_at_ms", MOBILE_AUTH_INVALID_BODY))),
    issued_at_ms: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileEpochMillis(bodyUnsigned(fields, "issued_at_ms", MOBILE_AUTH_INVALID_BODY))),
    limits: decodeMobileLimits(bodyValue(fields, "limits", MOBILE_AUTH_INVALID_BODY)),
    schema: exactString(bodyString(fields, "schema", MOBILE_AUTH_INVALID_BODY), MOBILE_AUTH_SCHEMA_V1, MOBILE_AUTH_SCHEMA_MISMATCH, "schema"),
    server_identity: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileServerIdentity(bodyString(fields, "server_identity", MOBILE_AUTH_INVALID_BODY))),
    session_scope: bodyStrings(fields, "session_scope", MOBILE_AUTH_INVALID_BODY, MAX_MOBILE_SESSIONS, MOBILE_AUTH_VALUE_INVALID).map((value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileSessionId(value)),
    ),
  };
}

/** Bounded secret-free operator credential inventory page. */
export interface MobileCredentialInventory {
  readonly credentials: readonly MobileCredentialSummary[];
  readonly next_cursor: MobileCredentialId | null;
  readonly schema: typeof MOBILE_AUTH_SCHEMA_V1;
}

/** The exact key set this nested body carries. */
export const MobileCredentialInventory_FIELDS: readonly string[] = [
  "credentials",
  "next_cursor",
  "schema",
];

export function decodeMobileCredentialInventory(body: JsonValue): MobileCredentialInventory {
  const fields = exactFields(body, MobileCredentialInventory_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    credentials: bodyArray(fields, "credentials", MOBILE_AUTH_INVALID_BODY, MAX_MOBILE_CREDENTIAL_PAGE_SIZE, MOBILE_AUTH_VALUE_INVALID).map(
      decodeMobileCredentialSummary,
    ),
    next_cursor: mapNullable(bodyStringOrNull(fields, "next_cursor", MOBILE_AUTH_INVALID_BODY), (value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialId(value)),
    ),
    schema: exactString(bodyString(fields, "schema", MOBILE_AUTH_INVALID_BODY), MOBILE_AUTH_SCHEMA_V1, MOBILE_AUTH_SCHEMA_MISMATCH, "schema"),
  };
}

/** Bounded operator credential inventory page request. */
export interface MobileCredentialInventoryRequest {
  readonly cursor: MobileCredentialId | null;
  readonly page_size: MobileCredentialPageSize;
}

/** The exact key set this nested body carries. */
export const MobileCredentialInventoryRequest_FIELDS: readonly string[] = [
  "cursor",
  "page_size",
];

export function encodeMobileCredentialInventoryRequest(value: MobileCredentialInventoryRequest): JsonValue {
  exactInputFields(value, MobileCredentialInventoryRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {kind: "object", entries: [
    ["cursor", ((entry) => entry === null
        ? {kind: "null"}
        : {kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialId(entry))})(value.cursor)],
    ["page_size", {kind: "integer", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialPageSize(value.page_size))}],
  ]};
}

export function decodeMobileCredentialInventoryRequest(body: JsonValue): MobileCredentialInventoryRequest {
  const fields = exactFields(body, MobileCredentialInventoryRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    cursor: mapNullable(bodyStringOrNull(fields, "cursor", MOBILE_AUTH_INVALID_BODY), (value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialId(value)),
    ),
    page_size: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialPageSize(bodyUnsigned(fields, "page_size", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Operator revocation of one exact credential family. */
export interface MobileCredentialRevokeRequest {
  readonly credential_id: MobileCredentialId;
}

/** The exact key set this nested body carries. */
export const MobileCredentialRevokeRequest_FIELDS: readonly string[] = [
  "credential_id",
];

export function encodeMobileCredentialRevokeRequest(value: MobileCredentialRevokeRequest): JsonValue {
  exactInputFields(value, MobileCredentialRevokeRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {kind: "object", entries: [
    ["credential_id", {kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialId(value.credential_id))}],
  ]};
}

export function decodeMobileCredentialRevokeRequest(body: JsonValue): MobileCredentialRevokeRequest {
  const fields = exactFields(body, MobileCredentialRevokeRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    credential_id: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialId(bodyString(fields, "credential_id", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Secret-free operator inventory projection for one credential family. */
export interface MobileCredentialSummary {
  readonly authorization: MobileAuthorization;
  readonly refresh_expires_at_ms: MobileEpochMillis;
  readonly revoked_at_ms: MobileEpochMillis | null;
}

/** The exact key set this nested body carries. */
export const MobileCredentialSummary_FIELDS: readonly string[] = [
  "authorization",
  "refresh_expires_at_ms",
  "revoked_at_ms",
];

export function decodeMobileCredentialSummary(body: JsonValue): MobileCredentialSummary {
  const fields = exactFields(body, MobileCredentialSummary_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    authorization: decodeMobileAuthorization(bodyValue(fields, "authorization", MOBILE_AUTH_INVALID_BODY)),
    refresh_expires_at_ms: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileEpochMillis(bodyUnsigned(fields, "refresh_expires_at_ms", MOBILE_AUTH_INVALID_BODY))),
    revoked_at_ms: mapNullable(bodyIntegerOrNull(fields, "revoked_at_ms", MOBILE_AUTH_INVALID_BODY), (value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileEpochMillis(value)),
    ),
  };
}

/** HTTPS-origin-bound discovery document. */
export interface MobileDiscovery {
  readonly credential_inventory_endpoint: MobileCredentialInventoryEndpoint;
  readonly credential_revoke_endpoint: MobileCredentialRevokeEndpoint;
  readonly operator_provision_endpoint: MobileOperatorProvisionEndpoint;
  readonly origin: MobileHttpsOrigin;
  readonly pairing_create_endpoint: MobilePairingCreateEndpoint;
  readonly pairing_exchange_endpoint: MobilePairingExchangeEndpoint;
  readonly platform_endpoint: MobilePlatformEndpoint;
  readonly protocol: typeof MOBILE_AUTH_PROTOCOL;
  readonly schema: typeof MOBILE_AUTH_SCHEMA_V1;
  readonly server_identity: MobileServerIdentity;
  readonly supported_versions: readonly MobileProtocolVersion[];
}

/** The exact key set this nested body carries. */
export const MobileDiscovery_FIELDS: readonly string[] = [
  "credential_inventory_endpoint",
  "credential_revoke_endpoint",
  "operator_provision_endpoint",
  "origin",
  "pairing_create_endpoint",
  "pairing_exchange_endpoint",
  "platform_endpoint",
  "protocol",
  "schema",
  "server_identity",
  "supported_versions",
];

export function decodeMobileDiscovery(body: JsonValue): MobileDiscovery {
  const fields = exactFields(body, MobileDiscovery_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    credential_inventory_endpoint: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialInventoryEndpoint(bodyString(fields, "credential_inventory_endpoint", MOBILE_AUTH_INVALID_BODY))),
    credential_revoke_endpoint: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileCredentialRevokeEndpoint(bodyString(fields, "credential_revoke_endpoint", MOBILE_AUTH_INVALID_BODY))),
    operator_provision_endpoint: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileOperatorProvisionEndpoint(bodyString(fields, "operator_provision_endpoint", MOBILE_AUTH_INVALID_BODY))),
    origin: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileHttpsOrigin(bodyString(fields, "origin", MOBILE_AUTH_INVALID_BODY))),
    pairing_create_endpoint: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingCreateEndpoint(bodyString(fields, "pairing_create_endpoint", MOBILE_AUTH_INVALID_BODY))),
    pairing_exchange_endpoint: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingExchangeEndpoint(bodyString(fields, "pairing_exchange_endpoint", MOBILE_AUTH_INVALID_BODY))),
    platform_endpoint: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePlatformEndpoint(bodyString(fields, "platform_endpoint", MOBILE_AUTH_INVALID_BODY))),
    protocol: exactString(bodyString(fields, "protocol", MOBILE_AUTH_INVALID_BODY), MOBILE_AUTH_PROTOCOL, MOBILE_AUTH_PROTOCOL_MISMATCH, "protocol"),
    schema: exactString(bodyString(fields, "schema", MOBILE_AUTH_INVALID_BODY), MOBILE_AUTH_SCHEMA_V1, MOBILE_AUTH_SCHEMA_MISMATCH, "schema"),
    server_identity: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileServerIdentity(bodyString(fields, "server_identity", MOBILE_AUTH_INVALID_BODY))),
    supported_versions: bodyArray(fields, "supported_versions", MOBILE_AUTH_INVALID_BODY, MAX_MOBILE_PROTOCOL_VERSIONS, MOBILE_AUTH_VALUE_INVALID).map((value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileProtocolVersion(jsonUnsigned(value, MOBILE_AUTH_INVALID_BODY))),
    ),
  };
}

/** Strict bounded mobile lifecycle refusal. */
export interface MobileError {
  readonly error: MobileErrorCode;
}

/** The exact key set this nested body carries. */
export const MobileError_FIELDS: readonly string[] = [
  "error",
];

export function decodeMobileError(body: JsonValue): MobileError {
  const fields = exactFields(body, MobileError_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    error: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileErrorCode(bodyString(fields, "error", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Server-negotiated per-credential mobile ceilings. */
export interface MobileLimits {
  readonly max_follow_up_bytes: MobileFollowUpBytes;
  readonly max_page_events: MobilePageEvents;
}

/** The exact key set this nested body carries. */
export const MobileLimits_FIELDS: readonly string[] = [
  "max_follow_up_bytes",
  "max_page_events",
];

export function encodeMobileLimits(value: MobileLimits): JsonValue {
  exactInputFields(value, MobileLimits_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {kind: "object", entries: [
    ["max_follow_up_bytes", {kind: "integer", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileFollowUpBytes(value.max_follow_up_bytes))}],
    ["max_page_events", {kind: "integer", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePageEvents(value.max_page_events))}],
  ]};
}

export function decodeMobileLimits(body: JsonValue): MobileLimits {
  const fields = exactFields(body, MobileLimits_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    max_follow_up_bytes: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileFollowUpBytes(bodyUnsigned(fields, "max_follow_up_bytes", MOBILE_AUTH_INVALID_BODY))),
    max_page_events: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePageEvents(bodyUnsigned(fields, "max_page_events", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Scope an operator explicitly provisions to one mobile client. */
export interface MobileOperatorProvisionRequest {
  readonly actions: readonly MobileAction[];
  readonly limits: MobileLimits;
  readonly session_scope: readonly MobileSessionId[];
}

/** The exact key set this nested body carries. */
export const MobileOperatorProvisionRequest_FIELDS: readonly string[] = [
  "actions",
  "limits",
  "session_scope",
];

export function encodeMobileOperatorProvisionRequest(value: MobileOperatorProvisionRequest): JsonValue {
  exactInputFields(value, MobileOperatorProvisionRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {kind: "object", entries: [
    ["actions", ((): JsonValue => {
        if (value.actions.length > MAX_MOBILE_ACTIONS) throw new RefusalError(MOBILE_AUTH_VALUE_INVALID, "array exceeds its ceiling");
        return {kind: "array", items: value.actions.map((item): JsonValue => ({kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => decodeMobileAction(item))}))};
      })()],
    ["limits", encodeMobileLimits(value.limits)],
    ["session_scope", ((): JsonValue => {
        if (value.session_scope.length > MAX_MOBILE_SESSIONS) throw new RefusalError(MOBILE_AUTH_VALUE_INVALID, "array exceeds its ceiling");
        return {kind: "array", items: value.session_scope.map((item): JsonValue => ({kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileSessionId(item))}))};
      })()],
  ]};
}

export function decodeMobileOperatorProvisionRequest(body: JsonValue): MobileOperatorProvisionRequest {
  const fields = exactFields(body, MobileOperatorProvisionRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    actions: bodyStrings(fields, "actions", MOBILE_AUTH_INVALID_BODY, MAX_MOBILE_ACTIONS, MOBILE_AUTH_VALUE_INVALID).map((value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => decodeMobileAction(value)),
    ),
    limits: decodeMobileLimits(bodyValue(fields, "limits", MOBILE_AUTH_INVALID_BODY)),
    session_scope: bodyStrings(fields, "session_scope", MOBILE_AUTH_INVALID_BODY, MAX_MOBILE_SESSIONS, MOBILE_AUTH_VALUE_INVALID).map((value) =>
      refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileSessionId(value)),
    ),
  };
}

/** Strict one-time pairing exchange proof. */
export interface MobilePairingExchangeRequest {
  readonly pairing_id: MobilePairingId;
  readonly pairing_token: MobilePairingToken;
  readonly server_identity: MobileServerIdentity;
}

/** The exact key set this nested body carries. */
export const MobilePairingExchangeRequest_FIELDS: readonly string[] = [
  "pairing_id",
  "pairing_token",
  "server_identity",
];

export function encodeMobilePairingExchangeRequest(value: MobilePairingExchangeRequest): JsonValue {
  exactInputFields(value, MobilePairingExchangeRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {kind: "object", entries: [
    ["pairing_id", {kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingId(value.pairing_id))}],
    ["pairing_token", {kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingToken(value.pairing_token))}],
    ["server_identity", {kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileServerIdentity(value.server_identity))}],
  ]};
}

export function decodeMobilePairingExchangeRequest(body: JsonValue): MobilePairingExchangeRequest {
  const fields = exactFields(body, MobilePairingExchangeRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    pairing_id: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingId(bodyString(fields, "pairing_id", MOBILE_AUTH_INVALID_BODY))),
    pairing_token: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingToken(bodyString(fields, "pairing_token", MOBILE_AUTH_INVALID_BODY))),
    server_identity: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileServerIdentity(bodyString(fields, "server_identity", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Copy-safe origin and identity-bound one-time pairing offer. */
export interface MobilePairingOffer {
  readonly exchange_endpoint: MobilePairingExchangeEndpoint;
  readonly expires_at_ms: MobileEpochMillis;
  readonly origin: MobileHttpsOrigin;
  readonly pairing_id: MobilePairingId;
  readonly pairing_token: MobilePairingToken;
  readonly schema: typeof MOBILE_AUTH_SCHEMA_V1;
  readonly server_identity: MobileServerIdentity;
}

/** The exact key set this nested body carries. */
export const MobilePairingOffer_FIELDS: readonly string[] = [
  "exchange_endpoint",
  "expires_at_ms",
  "origin",
  "pairing_id",
  "pairing_token",
  "schema",
  "server_identity",
];

export function decodeMobilePairingOffer(body: JsonValue): MobilePairingOffer {
  const fields = exactFields(body, MobilePairingOffer_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    exchange_endpoint: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingExchangeEndpoint(bodyString(fields, "exchange_endpoint", MOBILE_AUTH_INVALID_BODY))),
    expires_at_ms: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileEpochMillis(bodyUnsigned(fields, "expires_at_ms", MOBILE_AUTH_INVALID_BODY))),
    origin: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileHttpsOrigin(bodyString(fields, "origin", MOBILE_AUTH_INVALID_BODY))),
    pairing_id: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingId(bodyString(fields, "pairing_id", MOBILE_AUTH_INVALID_BODY))),
    pairing_token: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobilePairingToken(bodyString(fields, "pairing_token", MOBILE_AUTH_INVALID_BODY))),
    schema: exactString(bodyString(fields, "schema", MOBILE_AUTH_INVALID_BODY), MOBILE_AUTH_SCHEMA_V1, MOBILE_AUTH_SCHEMA_MISMATCH, "schema"),
    server_identity: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileServerIdentity(bodyString(fields, "server_identity", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Origin-pinned refresh or revocation request. */
export interface MobileRefreshRequest {
  readonly refresh_token: MobileRefreshToken;
  readonly server_identity: MobileServerIdentity;
}

/** The exact key set this nested body carries. */
export const MobileRefreshRequest_FIELDS: readonly string[] = [
  "refresh_token",
  "server_identity",
];

export function encodeMobileRefreshRequest(value: MobileRefreshRequest): JsonValue {
  exactInputFields(value, MobileRefreshRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {kind: "object", entries: [
    ["refresh_token", {kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileRefreshToken(value.refresh_token))}],
    ["server_identity", {kind: "string", value: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileServerIdentity(value.server_identity))}],
  ]};
}

export function decodeMobileRefreshRequest(body: JsonValue): MobileRefreshRequest {
  const fields = exactFields(body, MobileRefreshRequest_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    refresh_token: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileRefreshToken(bodyString(fields, "refresh_token", MOBILE_AUTH_INVALID_BODY))),
    server_identity: refuse(MOBILE_AUTH_VALUE_INVALID, () => MobileServerIdentity(bodyString(fields, "server_identity", MOBILE_AUTH_INVALID_BODY))),
  };
}

/** Confirmation that revocation completed server-side. */
export interface MobileRevocation {
  readonly revoked: boolean;
  readonly schema: typeof MOBILE_AUTH_SCHEMA_V1;
}

/** The exact key set this nested body carries. */
export const MobileRevocation_FIELDS: readonly string[] = [
  "revoked",
  "schema",
];

export function decodeMobileRevocation(body: JsonValue): MobileRevocation {
  const fields = exactFields(body, MobileRevocation_FIELDS, MOBILE_AUTH_INVALID_BODY);
  return {
    revoked: bodyBool(fields, "revoked", MOBILE_AUTH_INVALID_BODY),
    schema: exactString(bodyString(fields, "schema", MOBILE_AUTH_INVALID_BODY), MOBILE_AUTH_SCHEMA_V1, MOBILE_AUTH_SCHEMA_MISMATCH, "schema"),
  };
}
