// SPDX-License-Identifier: Apache-2.0

// GENERATED by automonique_protocol::codegen — do not edit by hand.
// Regenerate with: AUTOMONIQUE_PROTOCOL_REGENERATE=1 cargo test -p automonique-protocol --test codegen
//
// Source of truth: automonique_protocol::progress_api
// One normalized progress frame, as every surface renders a live run from.
//
// Rust is the wire source of truth. Hand-written SDK code may add
// ergonomics; it may not redefine anything in this file.

import {RunId} from "./admin-command.ts";
import {Authority, EpochMillis, SpoolSequence} from "./runs.ts";
import {ValidationError, byteLength} from "./runtime.ts";

/** Maximum canonical bytes of one encoded frame. It is inside the runner spool's own payload ceiling, because a frame is stored as one spool event's payload. */
export const MAX_PROGRESS_CANONICAL_BYTES = 40960;

/** Longest wait a retry context may advertise. A delay past it is refused rather than clamped: a wait nobody will sit through is a refusal wearing a promise. */
export const MAX_RETRY_AFTER_MS = 300000;

/** Stable schema identifier for the version-one frame. */
export const PROGRESS_API_SCHEMA_V1 = "automonique.progress/v1";

/** Stable protocol name for the normalized progress stream. */
export const PROGRESS_PROTOCOL = "automonique.progress";

/** Bounded string, at most 16384 UTF-8 bytes. */
export type ProgressText = string & {readonly __brand: "ProgressText"};
export const ProgressText_MAX_BYTES = 16384;
export const ProgressText_PATTERN = /^(?:[^\p{Cc}]|[\n\t])+$/u;
export function ProgressText(value: string): ProgressText {
  if (value.length === 0) throw new ValidationError("ProgressText", "empty");
  if (byteLength(value) > 16384) throw new ValidationError("ProgressText", "too_long");
  if (!ProgressText_PATTERN.test(value)) throw new ValidationError("ProgressText", "invalid_character");
  return value as ProgressText;
}

/** Bounded integer in [0, 300000]. */
export type RetryAfterMillis = bigint & {readonly __brand: "RetryAfterMillis"};
export const RetryAfterMillis_MIN = 0n;
export const RetryAfterMillis_MAX = 300000n;
export function RetryAfterMillis(value: bigint): RetryAfterMillis {
  if (value < 0n || value > 300000n) throw new ValidationError("RetryAfterMillis", "out_of_range");
  return value as RetryAfterMillis;
}

/** Bounded integer in [1, 4294967295]. */
export type RetryAttempt = bigint & {readonly __brand: "RetryAttempt"};
export const RetryAttempt_MIN = 1n;
export const RetryAttempt_MAX = 4294967295n;
export function RetryAttempt(value: bigint): RetryAttempt {
  if (value < 1n || value > 4294967295n) throw new ValidationError("RetryAttempt", "out_of_range");
  return value as RetryAttempt;
}

export type EventKind = "approval_requested" | "approval_resolved" | "assistant_message_completed" | "assistant_message_delta" | "provider_connected" | "provider_disconnected" | "provider_fault" | "provider_warning" | "run_terminal" | "session_created" | "session_loaded" | "session_updated" | "subagent_completed" | "subagent_event" | "subagent_started" | "tool_call_completed" | "tool_call_started" | "tool_call_updated" | "turn_completed" | "turn_interrupted" | "turn_queued" | "turn_started" | "turn_steered" | "usage_updated";
export const EventKind_VALUES: readonly EventKind[] = ["approval_requested", "approval_resolved", "assistant_message_completed", "assistant_message_delta", "provider_connected", "provider_disconnected", "provider_fault", "provider_warning", "run_terminal", "session_created", "session_loaded", "session_updated", "subagent_completed", "subagent_event", "subagent_started", "tool_call_completed", "tool_call_started", "tool_call_updated", "turn_completed", "turn_interrupted", "turn_queued", "turn_started", "turn_steered", "usage_updated"];
/** Security-sensitive: an undefined value is refused. */
export function decodeEventKind(value: string): EventKind {
  if (!(EventKind_VALUES as readonly string[]).includes(value)) {
    throw new ValidationError("EventKind", "unknown_enum_value");
  }
  return value as EventKind;
}

export type RetryCategory = "internal" | "overloaded" | "rate_limited" | "rejected" | "timeout" | "transport";
export const RetryCategory_VALUES: readonly RetryCategory[] = ["internal", "overloaded", "rate_limited", "rejected", "timeout", "transport"];
/** Security-sensitive: an undefined value is refused. */
export function decodeRetryCategory(value: string): RetryCategory {
  if (!(RetryCategory_VALUES as readonly string[]).includes(value)) {
    throw new ValidationError("RetryCategory", "unknown_enum_value");
  }
  return value as RetryCategory;
}

export type StepStatus = "completed" | "error" | "in_progress" | "pending";
export const StepStatus_VALUES: readonly StepStatus[] = ["completed", "error", "in_progress", "pending"];
/** Security-sensitive: an undefined value is refused. */
export function decodeStepStatus(value: string): StepStatus {
  if (!(StepStatus_VALUES as readonly string[]).includes(value)) {
    throw new ValidationError("StepStatus", "unknown_enum_value");
  }
  return value as StepStatus;
}

/** What one frame says beyond its kind. Every member is present and may be null; which of them a kind requires and which it forbids is a cross-field rule only the Rust constructor applies. */
export interface ProgressBody {
  readonly retry: RetryContext | null;
  readonly step: StepStatus | null;
  readonly text: ProgressText | null;
}
export const ProgressBody_FIELDS: readonly string[] = [
  "retry",
  "step",
  "text",
];

/** One normalized progress event. `sequence` is the runner spool's own position, which is what makes it a resumption cursor rather than a counter. */
export interface ProgressFrame {
  readonly at_ms: EpochMillis;
  readonly authority: Authority;
  readonly body: ProgressBody;
  readonly kind: EventKind;
  readonly run_id: RunId;
  readonly sequence: SpoolSequence;
}
export const ProgressFrame_FIELDS: readonly string[] = [
  "at_ms",
  "authority",
  "body",
  "kind",
  "run_id",
  "sequence",
];

/** Why a warning or a fault might be tried again, and when. A wait is present only on a retryable context; the Rust constructor refuses the other combination. */
export interface RetryContext {
  readonly attempt: RetryAttempt;
  readonly category: RetryCategory;
  readonly retry_after_ms: RetryAfterMillis | null;
  readonly retryable: boolean;
}
export const RetryContext_FIELDS: readonly string[] = [
  "attempt",
  "category",
  "retry_after_ms",
  "retryable",
];
