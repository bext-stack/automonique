<!-- SPDX-License-Identifier: Elastic-2.0 -->

# Owner decision — R1-02 bounded domain primitives contract

| Field | Decision |
|---|---|
| Status | pending external owner acceptance of the exact protected-control candidate commit and tree |
| Expected base | `38f57c7a3ab9d08baac0db908904ee0f56e787a2` on local and remote `main` |
| Work ID | contract preparation for `R1-02` — bounded domain primitives |
| Dependency evidence | `R0-18` is done; `plan/evidence/R0-18.json` has SHA-256 `c4eab4d870a2e04253a54cb6d3b1b7f8ad9f344bfcf7455b60e5fc55fa3d1a91`; `R1-02` depends only on `R0-18` and has no blocking gate |
| Objective | specify a dependency-free, bounded Rust foundation for opaque IDs, UTF-8 text, UTC epoch-millisecond timestamps, monotonic revisions, public HTTP(S) URLs and secret-bearing values without implementing product behavior in this candidate |
| Budget | one contract-preparation iteration, at most 30 minutes, with no Rust implementation, evidence, history, status, authority, gate, metric, baseline or generator-policy change |
| Allowed paths | `plan/owner-decisions/2026-08-10-r1-02-contract.md`, `plan/contracts/R1-02.md`, `plan/ready.md`, `.automonique/dev/program.yaml`, `.automonique/dev/objectives.json` |
| Checks | generated work-graph byte equality; plan integrity and self-test; generated program and guide/objective verification; exact scope, diff and licence/path hygiene |
| Licence class | `Elastic-2.0` |
| Stop conditions | changed local or remote base; dependency/gate disagreement; any path outside the exact five-path scope; generated drift; a failed check; an unbounded value; a secret-exposing trait or diagnostic; a new dependency or lockfile change; or absent exact-revision owner acceptance |

## Control boundary

This decision only makes the pre-existing `R1-02` graph node selectable by
adding its missing contract and regenerating the three affected derived
artifacts. It neither implements nor certifies `R1-02`, marks no work done,
changes no gate, and grants no release or deployment authority.

The candidate cannot use its own contract, budget, checks or generated
readiness state to accept or integrate itself. The policy at the expected base
continues to judge it. Local-main compare-and-swap and ordinary non-force
publication to `origin/main` require a contemporaneous external owner
acceptance naming the candidate's exact commit and tree. Any tip drift or
non-fast-forward outcome stops the operation.
