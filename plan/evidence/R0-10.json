{
  "item": "R0-10",
  "base": "eb0935adb4d4b26335ccc6d18f0dd747369b7e38",
  "scope": "partial \u2014 bind the drill to the canonical R0-09 producer and harden backup-manifest containment; clean-host execution and objective comparison remain unmeasured",
  "checks": [
    {
      "name": "Safety refusal",
      "command": "python3 spikes/recovery/test_recovery_drill.py",
      "result": "pass",
      "disposition": "pass",
      "output": "50/50 tests passed. Six workspace refusal codes retain positive and negative controls. Backup manifests reject unknown/missing/duplicate keys, non-integer counters, noncanonical relative paths, absolute paths, traversal, non-SHA-256 hashes, payload/root/target-parent symlinks, missing files and non-regular files before creating the restore target. Descriptor-relative O_NOFOLLOW reads/writes close source replacement races. Post-write device/inode revalidation closes target-parent replacement races and removes the fd-pinned target on refusal. Both deterministic race controls passed."
    },
    {
      "name": "Backup consistency",
      "command": "python3 spikes/recovery/test_recovery_drill.py",
      "result": "pass",
      "disposition": "pass",
      "output": "The existing SQLite online-backup drill still restores 40 events and 40 artifacts and passes all eight coherence invariants. The naive-order, torn-set, corrupt-database, missing/config-lag, orphan, hash-tamper and unreadable-database controls remain green. Restore now validates every source payload and containment rule before it creates or writes the target."
    },
    {
      "name": "Clean-host restore",
      "command": "python3 spikes/recovery/drill.py",
      "result": null,
      "disposition": "unmeasured",
      "reason": "This bounded slice does not create the disposable same-kernel namespace, execute a disconnected recovery startup, or prove source and repository inaccessibility. The current drill remains a clean-target local fixture and is not presented as clean-host evidence."
    },
    {
      "name": "Objective measurement",
      "command": "python3 spikes/recovery/drill.py",
      "result": null,
      "disposition": "unmeasured",
      "reason": "The local fixture still reports both timing comparisons out_of_scope. This slice consumes the canonical R0-09 objective declarations without claiming that its existing pacing or restore-only timer meets them."
    },
    {
      "name": "Dependency agreement",
      "command": "python3 spikes/recovery/dependencies.py --report (expected exit 2 for recorded gaps); python3 -m unittest -v spikes.recovery.test_dependencies_contract; python3 tools/surface_inventory/verify.py",
      "result": null,
      "disposition": "unmeasured",
      "reason": "Producer authenticity and consumption pass, but the existing drill does not yet emit one disposition receipt for each of the 21 positions. The report's expected exit 2 and 28 findings keep the full Dependency agreement row open.",
      "output": "The canonical producer plan/inventory/surface/restore-dependencies.json is authenticated and consumed: schema automonique.restore-dependencies/v1, work_item R0-09, consumer R0-10, 21 ordered positions, two minute objectives and one excluded class. Authenticity is separate from agreement: the report truthfully names 8 existing local-drill IDs absent from R0-09 and 20 canonical positions not yet exercised. The consumer verifies exact recursive shapes/types/enums, duplicate keys, contiguous topological order and earlier references, exact lexical path and source digest, then re-renders the actual R0-09 source and requires byte equality. Nine test methods passed, including threshold/source/order/path and symlink-alias refusals. Surface inventory verification passed with all derived views current."
    },
    {
      "name": "Rerun hygiene",
      "command": "python3 spikes/recovery/test_recovery_drill.py",
      "result": "pass",
      "disposition": "pass",
      "output": "Sequential reruns retain the same normalized outcome and leave no run-scoped workspace after success, inconsistency or injected failure. The skip-cleanup control still proves residue detection."
    },
    {
      "name": "Quality",
      "command": "python3 spikes/recovery/test_recovery_drill.py && python3 -m unittest -v spikes.recovery.test_dependencies_contract && python3 -m py_compile spikes/recovery/dependencies.py spikes/recovery/recovery_set.py spikes/recovery/test_dependencies_contract.py spikes/recovery/test_recovery_drill.py && git diff --check",
      "result": "pass",
      "disposition": "pass",
      "output": "50 recovery tests, 9 canonical-consumer tests and 59/59 discovery tests passed. Python compilation and diff whitespace checks passed. The implementation uses the standard library plus the exact checked-in R0-09 renderer module, pinned by full import-module/name/alias AST comparison; it performs no network fetch, production access or credential handling. The local drill now returns outcome=incomplete and exit 2 while the 28 agreement findings remain."
    }
  ],
  "review": {
    "reviewers": 1,
    "blocking_findings": 0,
    "acceptance": "accepted for partial typed integration after independent adversarial review; not accepted as R0-10 completion"
  },
  "remaining": [
    "Create and attest an exec-clean disposable same-kernel recovery boundary with source and repository content inaccessible.",
    "Execute or source-backed-disposition all 21 R0-09 positions, including disconnected startup, synthetic descriptor resolution, audience revalidation and verified-disabled transport/provider enablement.",
    "Measure scheduled synthetic RPO and full-boundary RTO against the canonical objectives; a miss must block verified outcome.",
    "Replace the local nine-row needs table as the drill's execution authority and update the procedure/report around the 21 canonical positions."
  ],
  "notes": "This candidate is intentionally partial. It resolves the producer/consumer mismatch and closes real restore traversal and replacement-race vulnerabilities, but it does not mark R0-10 done, close a gate, or claim production readiness. Three contract rows remain null with reasons and full completion is not requested."
}
