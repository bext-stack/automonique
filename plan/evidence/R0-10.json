{
  "item": "R0-10",
  "base": "14793cb1388c9318ece6d344bc6894d6bee72e18",
  "scope": "partial — add a live exec-clean same-kernel boundary probe and a permanently completion-ineligible canonical 21-position receipt model; neither is yet integrated with the restore",
  "checks": [
    {
      "name": "Safety refusal",
      "command": "python3 -m unittest discover -s spikes/recovery -p 'test_*.py'",
      "result": "pass",
      "disposition": "pass",
      "output": "81/81 recovery tests passed. The boundary has typed refusals for capability, clone, UID map, parent control, Landlock, privilege, exec, worker, report, size and timeout failures. Missing-control and timeout children are killed and reaped; a launcher-death test proves parent-death protection kills the child. Existing workspace and restore-containment controls remain green."
    },
    {
      "name": "Backup consistency",
      "command": "python3 spikes/recovery/test_recovery_drill.py",
      "result": "pass",
      "disposition": "pass",
      "output": "The existing SQLite online-backup drill still restores 40 events and 40 artifacts and passes all eight coherence invariants and all containment/race controls. This slice does not change that engine."
    },
    {
      "name": "Clean-host restore",
      "command": "python3 spikes/recovery/clean_boundary.py",
      "result": null,
      "disposition": "unmeasured",
      "reason": "The standalone probe creates an exec-clean same-kernel boundary, but this slice does not run the backup restore, invariants or disconnected recovery startup inside it. It is boundary-mechanism evidence, not the completed Clean-host restore row.",
      "output": "Live probe verified on Linux x86_64 with Landlock ABI 4: PID 1 and UID 0 under UID-only mapping; all seven parent/child namespace identities differ; no_new_privs=1; capability words are zero; repository read and TCP connect return EACCES; only report FD 3 survives; child exits 0 and is reaped. Evidence records no seccomp, a trusted inline worker and standalone-probe scope."
    },
    {
      "name": "Objective measurement",
      "command": "python3 spikes/recovery/drill.py",
      "result": null,
      "disposition": "unmeasured",
      "reason": "The boundary and receipt model are not yet integrated with the scheduled backup and restore, so neither RPO nor full-boundary RTO is newly measured or compared in this slice."
    },
    {
      "name": "Dependency agreement",
      "command": "python3 -m unittest -v spikes.recovery.test_recovery_plan; python3 spikes/recovery/dependencies.py --report (expected exit 2)",
      "result": null,
      "disposition": "unmeasured",
      "reason": "The receipt model requires one canonical ordered receipt and preserves unresolved-prerequisite semantics, but deliberately has no execution resolver and can never mark itself completion-eligible. The actual drill still reports 28 agreement gaps.",
      "output": "12/12 receipt-model tests passed. IDs, order and prerequisites come only through the strict R0-09 consumer. Receipts are hash-chained; all five external-effect flags remain false; every N/A refuses absent policy; required-unrun parents prevent exercised or verified-disabled children and advance only the receipt cursor, not semantic startup state."
    },
    {
      "name": "Rerun hygiene",
      "command": "python3 -m unittest -v spikes.recovery.test_clean_boundary",
      "result": "pass",
      "disposition": "pass",
      "output": "10/10 boundary tests passed, including bounded timeout, parent death, missing control, forged report, nonzero child exit and high inherited descriptor after lowered soft limit. Every child is reaped and no fallback runs. Existing drill residue controls remain green."
    },
    {
      "name": "Quality",
      "command": "python3 -m unittest discover -s spikes/recovery -p 'test_*.py' && python3 spikes/recovery/clean_boundary.py && python3 -m py_compile spikes/recovery/*.py && git diff --check",
      "result": "pass",
      "disposition": "pass",
      "output": "81/81 discovery tests passed: 50 original recovery, 9 canonical consumer, 10 live boundary and 12 receipt model. Direct boundary execution verified and was reaped at status 0. Python compilation and whitespace checks passed. New modules use only the standard library and checked-in sibling consumer; no external dependency, network fetch, production access or credential value was introduced."
    }
  ],
  "review": {
    "reviewers": 2,
    "blocking_findings": 0,
    "acceptance": "accepted only as two standalone partial mechanisms after adversarial review; not accepted as R0-10 completion"
  },
  "remaining": [
    "Package the trusted recovery worker and run the actual restore, invariants and canonical receipt resolver inside the proven boundary.",
    "Execute all 21 R0-09 positions, including disconnected startup, synthetic descriptor resolution, audience revalidation and verified-disabled transport/provider enablement.",
    "Measure scheduled synthetic RPO and full-boundary RTO against the canonical objectives; a miss must block verified outcome.",
    "Replace the local nine-row needs table as the drill authority and bind real evidence documents to the canonical receipt model."
  ],
  "notes": "This candidate is intentionally partial. The live boundary and receipt model are independent mechanisms and are not presented as an integrated clean-host restore. Three contract rows remain null with reasons; R0-10 stays open and no production-readiness, deployment or transport claim is made."
}
