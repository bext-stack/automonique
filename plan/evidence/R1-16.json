{
  "item": "R1-16",
  "status": "partial",
  "base": "34c60053443e4db0a8740fbf12115390b61a3b43",
  "checks": [
    {
      "name": "Installation resolution",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector installation_resolution",
      "result": "pass",
      "output": "3 tests executed and passed, unchanged from the previous audit and re-run here. A bound installation (platform teams, application app-1, owner ext-tenant-1) resolves to TenantResolution::Resolved{tenant:\"acme\"}. An empty registry and a registry bound to a different owner both return TenantResolution::InstallationRequired. TenantResolution still has exactly two variants, so there is no Default/Fallback variant to land on. Rebinding the same key replaces rather than appending (resolve returns globex after a second bind). This slice did not touch installation resolution; the only change nearby is the addition of InstallationKey::application() and ::installation_owner() read accessors, which resolution does not consult."
    },
    {
      "name": "Identity resolution",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector identity_resolution && cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test identity external_identity_keys",
      "result": "pass",
      "output": "connector: 2 tests pass. IdentityResolution has exactly two variants, Linked{actor} and Unlinked{link_prompt}; the unlinked case carries a link prompt and never yields an Actor. identity: 3 tests pass, including every_mutable_attribute_is_refused_as_a_key, which iterates all 5 MutableAttribute values and gets IdentityError::MutableAttributeAsKey for each. Recorded as it was in the previous audit rather than restated as if this slice proved it: the mutable-handle half of this row is enforced in src/identity.rs (R1-13's module, not owned here), not in src/connector.rs; connector.rs has no path from an external handle to an IdentityResolution at all, so the connector layer contributes the enum and the identity layer contributes the refusal."
    },
    {
      "name": "Source-key stability",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector source_key_stability; then 5 separate OS process invocations of a scratch binary linking libautomonique_protocol-f9919e2c7dee2777.rlib and printing SourceKey::derive(teams/app-1/ext-tenant-1, \"evt-1\"); then a rustc compile probe passing a millisecond timestamp as a third argument",
      "result": "pass",
      "output": "3 in-crate tests pass (10 in-process derivations agree; a different event id, a different installation owner and a different platform each give a different key). Cross-process, re-measured for this slice: five independent process invocations each printed exactly `teams:app-1:ext-tenant-1:evt-1` (uniq -c reported `5 teams:app-1:ext-tenant-1:evt-1`), so stability across restarts and connector instances is measured, not asserted. The compile probe (probes/p6_source_key_clock.rs) fails with E0061 \"this function takes 2 arguments but 3 arguments were supplied\", so no clock, random value or process identity has an argument to enter through."
    },
    {
      "name": "Acknowledgement split",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector acknowledgement_split; then a rustc compile probe returning an Acknowledgement from a function declared to return BusinessAcceptance",
      "result": "pass",
      "output": "2 tests pass: Acknowledgement::immediate/deferred are distinguishable, BusinessAcceptance::for_input(\"input-1\") succeeds and for_input(\"\") is refused, so acceptance requires a durable input identity. The substitution probe (probes/p5_ack_for_acceptance.rs), rebuilt against the current rlib, fails with E0308 mismatched types, so an acknowledgement cannot be returned where an acceptance is required."
    },
    {
      "name": "Render intents",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector render_intents platform_type_isolation::outbound_content_is_an_intent_rather_than_a_payload; cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --doc; then three rustc compile probes: the audit's RenderIntent::terminal(<AdaptiveCard/>) call, a direct IntentPhrase(String) construction plus a String->IntentPhrase conversion, and RenderIntent::derived_from applied to an unrecorded EventOutcome",
      "result": "pass",
      "output": "Previously failing; now closed by changing the representation rather than adding a check to the old one. (1) Markup: outbound wording is no longer a String field. RenderIntent, EventOutcome and DurableEvent carry IntentPhrase, a newtype whose single field is private and whose only constructor is IntentPhrase::parse, which refuses the ten MARKUP_CHARACTERS ('<' '>' '&' '{' '}' '[' ']' '\"' '`' '\\\\') as well as empty, over-4096-byte and control-character input. The audit's exact probe now fails to compile: probes/p1_markup_string_constructor.rs gets E0599 \"no function or associated item named `terminal` found for struct `RenderIntent`\", and probes/p2_phrase_backdoor.rs gets both E0423 \"cannot initialize a tuple struct which contains private fields\" and E0277 \"the trait bound `IntentPhrase: From<String>` is not satisfied\", so parse is the only door and no value of the type can hold markup. The runtime table test refuses 8 markup-shaped inputs (the Adaptive Card element, an Adaptive Card JSON body, a Discord interaction-response JSON body, an HTML tag, an HTML entity, a Markdown link, a code fence and a backslash escape), each with category markup_rejected, and accepts the same sentence in prose. (2) Derivation: RenderIntent is a struct with three private fields and its only constructor is RenderIntent::derived_from(&DurableEvent); probes/p3_invented_intent.rs, which hands it an EventOutcome nobody recorded, fails with E0308. Every derived intent carries the source event id and the revision the event was recorded at, asserted for all four kinds (progress, clarification, approval, terminal). Mutation-checked three ways: neutralising the MARKUP_CHARACTERS scan made 2 tests fail; adding a RenderIntent::invented(&EventOutcome) constructor made the paired compile_fail doc test at connector.rs line 44 fail; making the IntentPhrase field public made the paired compile_fail doc test at line 23 fail; substituting Revision::FIRST for the event's revision made every_intent_kind_is_derived_from_a_durable_event fail. All four mutations were reverted and the suite re-run green. Both compile_fail doc tests are paired with a passing doc test that differs only in the rejected thing (parse instead of the private field; a recorded DurableEvent instead of a bare outcome). Honest limit: IntentPhrase excludes the character classes markup and serialized payloads need, not every conceivable sentence that names a platform model; a phrase reading \"AdaptiveCard v1.5\" in plain words is still accepted, because it is prose."
    },
    {
      "name": "Action-token binding",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector action_token_binding",
      "result": "pass",
      "output": "5 tests pass, unchanged by this slice. stored_form() does not contain the minted secret, starts with the marker 'h:' and is deterministic. resolve() at the bound revision succeeds; at revision 4 it returns ConnectorError::TargetRevisionChanged{bound_to:3,current:4} rather than re-targeting; at or past expiry it returns TokenExpired; for a different actor id and for a same-id actor in another tenant it returns ActorNotEligible; expired.category() != ineligible.category() (token_expired vs actor_not_eligible). The previous audit's caveat is carried forward rather than dropped: stored_form is a 64-bit FNV-1a fold that the module doc itself calls \"a non-cryptographic stand-in for the real digest\", so \"stored hashed\" holds only in the weak sense that the stored value is not the token; it is not preimage- or collision-resistant. It was not upgraded here because automonique-protocol carries no dependencies and this item may not add one, and hand-rolling SHA-256 is outside both the failing rows and the lease's purpose."
    },
    {
      "name": "Artifact grants",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector artifact_grants; then a rustc compile probe passing \"/etc/shadow\" as a fifth argument to ArtifactGrant::issue",
      "result": "pass",
      "output": "2 tests pass: a grant carries direction, an ArtifactDigest, a byte ceiling and an expiry, and is invalid at its expiry instant. MAX_GRANT_BYTES (536870912) is accepted and MAX_GRANT_BYTES+1 is refused with category grant_too_large, so an unbounded payload is unrepresentable. probes/p7_grant_path.rs, rebuilt against the current rlib, fails with E0061 \"this function takes 4 arguments but 5 arguments were supplied\"; the struct has no String field at all, and its only non-numeric field is ArtifactDigest, whose constructor (src/release.rs) rejects anything that is not a known algorithm plus exactly hex_len lowercase hex characters, so neither a filesystem path nor a credential-bearing URL can be smuggled through it."
    },
    {
      "name": "Coordinate preservation",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector coordinate_preservation; cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --doc; then a rustc compile probe printing all seven coordinates back out of a ConversationCoordinates",
      "result": "pass",
      "output": "Previously failing on three of the seven named coordinates; now closed. ConversationCoordinates gained installation() -> &InstallationKey, platform() -> &str and locale() -> Option<&str>, and InstallationKey gained application() and installation_owner(); all are read accessors, so nothing observable changed for an existing caller. The audit's failing read-back now compiles and runs: probes/p4_coordinates_readback.rs prints `teams|app-1|ext-tenant-1|conv-1|thread-1|msg-1|en-GB` and `mentioned=true`, so platform, installation, conversation, thread, message, locale and mention context all go in and all come back out. 4 tests pass: every_named_coordinate_round_trips asserts all seven; a_platform_without_threads_or_a_locale_is_representable asserts thread None and locale None round-trip as absence rather than as a lost value; an_edit_revises_and_a_deletion_tombstones keeps the previous variant check; a_tombstone_is_recorded_beside_the_history_not_in_place_of_it uses the new append-only SourceMessageLog and asserts that after Revised(2), Revised(3), Tombstoned(1000) the log still holds all three in arrival order and its coordinates are intact, which is the \"a deletion does not erase an audit record\" clause measured rather than asserted. Mutation-checked: locale() returning None made 2 tests fail; application() returning the installation owner made every_named_coordinate_round_trips fail; making record() clear the log on a tombstone made the tombstone test fail; changing events() to return an owned Vec made the paired compile_fail doc test at connector.rs line 70 fail. All reverted and re-run green. Honest limit on the \"without rewriting approved actions\" clause: it is held structurally, not by a positive test. SourceMessageEvent and SourceMessageLog have no field, argument or return type of any approval type, and the log exposes only a shared slice; a test asserting that a token still resolves after a tombstone is recorded would pass no matter what this module did, so it was written, judged decorative, and deleted rather than left in as evidence."
    },
    {
      "name": "Platform-type isolation",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector platform_type_isolation",
      "result": "pass",
      "output": "2 tests pass. no_platform_specific_type_is_declared_in_the_core_protocol reads every .rs file in crates/automonique-protocol/src (the test asserts at least 10 were scanned) and looks for struct/enum/type/trait declarations of 10 platform names: AdaptiveCard, TeamsActivity, Activity, DiscordInteraction, InteractionResponse, MessageComponent, ActionRow, SlashCommand, EmbedBuilder, BotFrameworkActivity. Zero findings, including across the types this slice added (IntentPhrase, EventOutcome, DurableEvent, SourceMessageLog, ConformanceObligation). outbound_content_is_an_intent_rather_than_a_payload was rewritten for the new representation and now derives an intent from a durable event and confirms an Adaptive Card body is not a parsable phrase. The previous audit's caveat still stands: the scan is over declaration text in the crate's sources, not over an enumeration of the exported public API, so a platform type re-exported from elsewhere under a different spelling would not be caught; nothing of that kind exists today because the crate has no dependencies."
    },
    {
      "name": "Quality",
      "command": "cargo fmt --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml --all -- --check; cargo clippy --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml --workspace --all-targets --offline -- -D warnings; cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml --workspace --offline --no-fail-fast; cargo check --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml --workspace --all-targets --offline --locked",
      "result": "fail",
      "output": "fmt exits 0 with an empty diff. Strict all-target Clippy over the whole workspace exits 0 with zero diagnostics, as does the narrower -p automonique-protocol --all-targets form. `cargo check --workspace --all-targets --offline --locked` exits 0, which is a mechanical measurement that the lockfile did not need updating; no Cargo.toml or Cargo.lock was opened by this work. automonique-protocol alone is green: 813 passed, 0 failed across its 25 integration test binaries plus 133 doc tests. The workspace as a whole is NOT green: the best run measured 998 passed, 4 failed. None of the four is in this item's lease and none is in connector.rs. Two are the pre-existing automonique-lab failures the previous audit already recorded (operational_claim_is_accepted_by_rust_status_and_selection and admitted_cli_allocates_and_replays_the_packet_bound_worktree, both selecting work item R0-19, which the generated program marks blocked by GATE-HARNESS). Two are in automonique-protocol test files another agent owns and was editing during this session: tools.rs hook_class_powers::ordering_timeout_and_failure_policy_are_declared_and_deterministic and namespace.rs no_escape_hatch::a_suppression_comment_cannot_disable_the_gate. The row as written measures the workspace and the workspace fails, so it is recorded as fail rather than scoped down to the crate. Two measurement hazards are recorded rather than hidden: (a) sibling agents were compiling the same target directory throughout, and several runs produced spurious failures whose only error was `extern location for automonique_protocol does not exist: .../libautomonique_protocol-f9919e2c7dee2777.rlib` — a build race, not a test result; runs containing that string were discarded and re-run rather than reported; (b) for the same reason several intermediate clippy and build runs failed inside sibling modules mid-edit (namespace.rs E0063, automation.rs E0493) and were retried rather than repaired. No new #[allow], todo!, unimplemented!, dbg! or unsafe appears in either owned file (grep over both returns nothing), and the crate still sets unsafe_code = \"forbid\". No network access was made; every cargo invocation used --offline."
    }
  ],
  "review": {
    "reviewers": 0,
    "blocking_findings": 0,
    "acceptance": "No independent reviewer examined this item. The previous version of this file was a third-party audit by another agent; this version is the implementer's own re-measurement after changing the code, which is a self-review and must not be counted as independent. Nothing here has been reviewed by a human."
  },
  "extra_checks": [
    {
      "name": "Conformance obligation list",
      "command": "cargo test --manifest-path /home/infra-sj278/automonique/rust/Cargo.toml -p automonique-protocol --offline --test connector conformance_obligations",
      "result": "pass",
      "output": "Previously failing with zero matches. The objective's last bullet — \"the conformance obligations are declared as a checkable list so a connector can be judged against the same contract regardless of platform, including restart resumption without replaying an external effect\" — is now a ConformanceObligation enum with ALL: [Self; 9], a stable as_str() spelling and a description() saying what a connector must demonstrate. The list includes ResumeWithoutReplayingExternalEffects, whose description is \"a restart resumes from the recorded source keys without re-sending a message already sent\". 1 test passes: the list has 9 entries, no two share a spelling, every entry has a non-empty description, and the restart-resumption entry is present. Mutation-checked by removing that entry from ALL, which made the test fail; reverted. This is not a row of the verification table, so it does not gate the item; it is recorded here because it is part of the objective."
    },
    {
      "name": "TypeScript connector surface",
      "command": "find /home/infra-sj278/automonique/sdk -type f; grep -rl \"connector\\|Connector\" /home/infra-sj278/automonique/sdk",
      "result": "fail",
      "output": "Unchanged and not addressed. sdk/typescript/packages/protocol contains only canonical.ts, index.ts, the conformance runner and its type shims; there is no channel-connector surface. The only files matching \"connector\" are sdk/typescript/packages/lab/src/bun-unix-connector.ts and its test, which is a unix-socket transport, not a platform channel bridge. The contract cites § TypeScript SDK connector surface as a source but the verification table has no cross-language row, so no parity is claimed. sdk/ is outside the three files this worker owns, so nothing was attempted there."
    }
  ],
  "notes": "Grading rule used throughout, carried over from the previous audit: a row is \"pass\" only when every clause of its Expected column was exercised by a command that actually ran; a clause this layer cannot express is reported rather than waved through. Two rows moved from fail to pass by changing the representation, not by adding a check to the old one. Render intents: RenderIntent stopped being an enum of String-carrying variants and became a struct with private fields whose only constructor takes a DurableEvent, and all outbound wording became IntentPhrase, a parse-only newtype; RenderIntent::progress and RenderIntent::terminal were deleted, which is the change that makes the audit's probe fail to compile. Coordinate preservation: three read accessors were added on ConversationCoordinates and two on InstallationKey, plus an append-only SourceMessageLog. Quality still fails, for causes entirely outside this lease (two pre-existing automonique-lab failures against a blocked R0-19, and two failures in protocol test files another agent owns and was mid-edit on). Every test claimed as closing a row was mutation-checked: 9 mutations were applied one at a time, each confirmed to turn the suite red, and each reverted before the next; the final state was re-run green (connector: 28 passed, 0 failed; doc tests: 133 passed, 0 failed; clippy: 0 diagnostics; fmt: no diff). One test was written and then deleted for being decorative rather than left in as evidence (see the Coordinate preservation note). base was read from .git/refs/heads/main; this worker never ran git, main advanced during the session as other agents' work landed, and the working tree carried uncommitted sibling-module edits throughout, so that sha names the tip at the time of writing rather than a tree that was ever quiescent. Compile probes were built with rustc --edition 2024 --extern automonique_protocol=<rust/target/debug/deps/libautomonique_protocol-f9919e2c7dee2777.rlib> against scratch files under the session scratchpad; the only repository files this work created or modified are rust/crates/automonique-protocol/src/connector.rs, rust/crates/automonique-protocol/tests/connector.rs and this evidence file. Fixtures use teams/discord, app-1, ext-tenant-1/2, acme, globex, conv-1, thread-1, msg-1, evt-1/2, en-GB and example.invalid only; no real tenant, guild, team, channel or user identifier appears. GATE-SCRUB's protected rule bundle is not installed (see plan/evidence/BOOT-003.json), so no scrub scan was run.",
  "adversarial_review": {
    "reviewer": "automated adversarial agent, separate session from the implementer",
    "independent": false,
    "method": "copied the crate, applied behavioural mutations, re-ran the suite to find rows whose tests survive the mutation",
    "caveat": "This review found surviving mutations for rows this file records as \"pass\". A \"pass\" here means the row was measured and observed to hold, not that its test would fail if the behaviour were removed. Read the findings before treating any row as closed.",
    "findings": "I made no edits to the repository; all mutation work was done in a copy at `/tmp/claude-1025/-home-infra-sj278-automonique/189dce1c-a957-463e-bb89-3fdb77b0d452/scratchpad/rust`. Repo baseline confirmed green: `cargo test -p automonique-protocol --offline --test connector` = 28 passed, 0 failed.\n\nVERDICT PER ROW\n\n| Row | Evidence says | My verdict |\n|---|---|---|\n| Installation resolution | pass | genuinely closed |\n| Identity resolution | pass | NOT closed at this layer (disclosed in the output field, but the label is wrong) |\n| Source-key stability | pass | NOT pinned by the named command — two mutations survive |\n| Acknowledgement split | pass | closed (one undisclosed residual) |\n| Render intents | pass | mostly closed, one concrete hole + a false doc claim |\n| Action-token binding | pass | revision/expiry/actor closed; target binding unenforced and unmeasured |\n| Artifact grants | pass | genuinely closed |\n| Coordinate preservation | pass | closed but weakly pinned |\n| Platform-type isolation | pass | NOT closed — two mutations survive |\n| Quality | fail | honestly recorded, no issue |\n| Conformance obligations (extra) | pass | closed as described |\n| TypeScript surface (extra) | fail | honestly recorded |\n\nDEFECTS, most severe first\n\n1. Platform-type isolation is measurable as failing. `tests/connector.rs:507-548` greps for the literal text `\"<keyword> <PlatformName>\"` in `src/*.rs`. The contract row demands the property be \"proven by the crate's public surface\"; nothing about the public surface is enumerated.\n   - Surviving mutation A: appended to `src/connector.rs` itself — `pub enum OutboundPayload { AdaptiveCard { body: String }, InteractionResponse { body: String } }`. Declaration text is `enum OutboundPayload`, so no forbidden string matches. `--test connector platform_type_isolation` = 2 passed. A serialized Adaptive Card and a Discord interaction response are now public core-protocol values.\n   - Surviving mutation B: `src/teams/mod.rs` containing `pub struct AdaptiveCard { pub body: String }` and `pub enum TeamsActivity {...}`, exported by adding `pub mod teams;` to `src/lib.rs`. `std::fs::read_dir` at line 525 is non-recursive, so the file is never opened; `assert!(scanned >= 10)` still holds (23 files). Both tests passed.\n   - Minor, same test: `\"Activity \"` at line 512 is `.trim()`ed at line 536, so the trailing space that was meant to prevent prefix matches is discarded. The check is now `contains(\"struct Activity\")`, which will fire on any sibling module declaring e.g. `struct ActivityLog` — a test in R1-16's file failing for a cause outside R1-16's lease.\n\n2. Source-key stability: the row's own command cannot detect a clock or a process identity in the key. `src/connector.rs:323-332` is correct today, but `tests/connector.rs:137-180` does not pin it.\n   - Surviving mutation: `SourceKey::derive` emitting `\"{platform}:{app}:{owner}:{clock}:{event}\"` with `clock` = `SystemTime::now()` millis. All 3 `source_key_stability` tests pass, and the full suite passes 28/28 — the ten in-process derivations land in the same millisecond, `assert_ne!` comparisons are unaffected, and `a_key_takes_no_time_or_process_input` (lines 172-180) only asserts `contains(\"teams\")` and `ends_with(\"evt-1\")`, both still true with the clock in the middle.\n   - Same result substituting `std::process::id()` for the clock: 28/28 pass. Process identity is constant within a test binary, which is exactly the \"across restarts and connector instances\" property the row claims.\n   - The evidence's compile probe (E0061, arity) only rules out passing a clock *as an argument*; it cannot detect `derive` reading one. The only measurement that would catch this is the five out-of-process invocations, which exist as a scratch binary outside the repo and cannot be re-run by a reviewer or by the gate command.\n\n3. Render intents: pre-rendered markup is still representable inside `RenderIntent`, and the doc claims otherwise. `src/connector.rs:543-551` states \"there is no content field ... so a platform model string and pre-rendered markup are not values this type can hold.\" `RenderIntent.source_event` (line 554) is a `String` filled from `DurableEvent::record`, whose `event_id` is checked by `bounded` (line 516) — empty/length/control only, no `MARKUP_CHARACTERS` scan. Ran in the copy:\n   `RenderIntent::derived_from(&DurableEvent::record(\"<AdaptiveCard><TextBlock>done</TextBlock></AdaptiveCard>\", Revision::new(3), outcome).unwrap()).source_event()` returns the Adaptive Card body verbatim (printed by the probe). That is the audit's exact payload, entering the same struct through a neighbouring field. `IntentPhrase` itself is sound — the compile_fail at line 23 is load-bearing (making the field `pub` turned that doc test FAILED, verified) and the three compile_fail doc tests each fail for their intended reason (E0423 private field, E0308 mismatched types, E0599 `clear` on `&[SourceMessageEvent]`), each with a paired passing example. The hole is the unvalidated `event_id`, not the phrase type.\n\n4. Render intents, second doc overclaim: `src/connector.rs:28-30` and `493-497` say an outcome \"a connector assembled for itself has nowhere to go\". `DurableEvent::record` is public and constrains nothing but the id, so the p3 probe is defeated by one extra line. Ran: a fabricated `DurableEvent::record(\"evt-i-made-up\", Revision::new(999), Finished{ summary: \"deployment finished successfully\" })` yields `intent.kind() == \"terminal\"` with no event recorded anywhere. \"Derived from a durable event\" is currently \"wrapped in a struct named DurableEvent\".\n\n5. Action-token target binding is neither enforced nor measured. `resolve` (`src/connector.rs:662-684`) checks expiry, actor and revision, and never looks at `self.target`. No test in `tests/connector.rs:308-378` calls `target()`.\n   - Surviving mutation: `mint` storing `target: \"any-target\".to_owned()` and discarding its argument — 28/28 pass.\n   - Consequence: a token minted for `approval-9` at revision 3 resolves successfully when presented against `approval-10`, provided the revision matches and the actor is eligible. The contract's \"binds to an exact target ... rather than a re-targeted action\" holds only for the revision component.\n\n6. Identity resolution is labelled pass but has no behaviour to test. `IdentityResolution` (line 295) is constructed only in `tests/connector.rs:111` and `124`; no function in the workspace maps an `ExternalIdentityKey` to it. Both tests build a variant and assert it is the variant they built, so no mutation of `src/connector.rs` short of deleting the enum can make them fail. Contrast `InstallationRegistry`, which has a real `bind`/`resolve`. The mutable-handle half lives in `src/identity.rs` (R1-13). By the evidence's own stated grading rule this should be `null` with a reason or partial, not `pass`; the output field discloses the situation honestly, which is why I rank this below the three above.\n\n7. Coordinate preservation is pinned only at one fixture. `assert_eq!(coordinates.installation(), &installation())` (tests line 429) does genuinely pin the stored key by structural equality, and the append-only tombstone test is real. But every accessor is exercised against exactly one value.\n   - Surviving mutation: `InstallationKey::platform/application/installation_owner` and `ConversationCoordinates::conversation/message` all rewritten to return the fixture constants (`\"teams\"`, `\"app-1\"`, `\"ext-tenant-1\"`, `\"conv-1\"`, `\"msg-1\"`), ignoring `&self` — 28/28 pass. Only `thread()` and `locale()` are pinned, because two fixtures differ there. A second fixture (discord / app-2 / ext-tenant-2 / conv-2 / msg-2) would close this.\n\n8. `MARKUP_CHARACTERS` is not the only door for outbound wording, contradicting `src/connector.rs:13-14` (\"Wording enters through `IntentPhrase::parse` and nowhere else\"). `IdentityResolution::Unlinked { link_prompt: String }` (line 304) is a public variant field holding text a platform user is shown; ran in the copy, it accepts `\"\\u{7}<b>x</b>\"` repeated to 9000 bytes — markup, control character and 35x the phrase ceiling. `TenantResolution::Resolved { tenant: String }` (line 242) is the same shape: `bind` rejects a 300-byte tenant, but `TenantResolution::Resolved { tenant: \"t\".repeat(300) }` constructs fine. Both are the only two types in the module that use public fields instead of a private field plus a validating constructor.\n\n9. Minor, undisclosed: `BusinessAcceptance::for_input` (line 385) treats \"a durable input identity\" as any non-empty, non-control string of ≤256 bytes, so `for_input(\"x\")` succeeds. The type-distinctness half of the row is sound (no `From`/`Into` between `Acknowledgement` and `BusinessAcceptance`); the durability half is a non-emptiness check. Worth a line in the evidence the way the FNV caveat got one.\n\nWHAT IS SOUND\n\n`IntentPhrase` as a parse-only newtype, the three compile_fail doc tests (all fail for the targeted reason, all paired with a passing example, and the line-23 one verified load-bearing by mutation), `InstallationRegistry` resolution including rebind semantics, `ArtifactGrant` (no path/URL/payload field is representable, and `ArtifactDigest::new` in `src/release.rs:259-281` genuinely requires a known algorithm plus exact-length lowercase hex), the append-only `SourceMessageLog` and its shared-slice guarantee, `ActionToken` expiry/actor/revision distinctness, and the conformance obligation list. The Quality row is correctly recorded as `fail` rather than scoped down, the FNV hash caveat and the structural-only \"approved actions\" caveat are disclosed rather than hidden, and the `review.acceptance` field correctly refuses to call a self-re-measurement independent.\n\nREPRODUCTION\n\nMutations were applied one at a time to `/tmp/claude-1025/-home-infra-sj278-automonique/189dce1c-a957-463e-bb89-3fdb77b0d452/scratchpad/rust/crates/automonique-protocol/src/connector.rs` (pristine copy kept at `.../scratchpad/connector.rs.orig`), each reverted before the next; probes ran as `.../scratchpad/rust/crates/automonique-protocol/tests/zz_reviewer_probe.rs` and `.../scratchpad/probes/cf{1,2,3}_*.rs`. The scratchpad tree was diffed back against the repo after reverting: `src` identical."
  }
}
