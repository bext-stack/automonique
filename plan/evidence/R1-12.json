{
  "item": "R1-12",
  "base": "48682a2",
  "checks": [
    {
      "name": "Global ordering",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal global_ordering",
      "result": "pass",
      "output": "Two tests pass. After appending event ids 1 and 2, offering 2, 1 and 0 each fails with JournalError::EventIdNotIncreasing { last: 2, offered }, and 3 is accepted, leaving three events. A refused append is not partially applied: appending a duplicate id 5 to a one-event journal errors and journal.events().len() stays 1. Re-measured unchanged at this base (2 passed, 0 failed); neither these tests nor Journal::append were touched by the current pass."
    },
    {
      "name": "Revision algebra",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal revision_algebra",
      "result": "pass",
      "output": "Four tests pass. Revisions 1, 2, 3 append in order and revision_of reports 3. A gap (offering 3 after 1) errors with JournalError::RevisionConflict { expected: 2, observed: 3 } and a repeat (offering 1 after 1) with { expected: 2, observed: 1 }, so both directions name expected and observed. A first event at revision 7 on a fresh aggregate errors with { expected: 1, observed: 7 }. Revisions are per aggregate: w-1 reaches 2 while w-2 is at 1 and an unknown aggregate reports None. Re-measured unchanged at this base (4 passed, 0 failed); this row was not touched by the current pass."
    },
    {
      "name": "Schema independence",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal schema_independence",
      "result": "pass",
      "output": "Three tests pass. DomainEvent::decodable_within takes a codec::VersionRange supplied by the reader, entirely separate from any table schema range: with a declared readable range of 2..=4, versions 2, 3 and 4 decode and versions 1 and 5 fail closed with JournalError::EventSchemaOutOfRange { supported_min: 2, supported_max: 4, offered }. EventSchemaVersion::new(0) is refused with category 'field_invalid', so a zero event schema version is not constructible. Re-measured unchanged at this base (3 passed, 0 failed); this row was not touched by the current pass."
    },
    {
      "name": "Outcome vocabulary",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal outcome_vocabulary",
      "result": "pass",
      "output": "Five tests pass, re-measured unchanged at this base (5 passed, 0 failed). Closed in the previous pass and not touched by this one: a private fn wire_spelling(ActionOutcome) -> &'static str matches exhaustively over the enum and returns the required literal, and const EVERY_OUTCOME names the six variants one at a time, so a seventh variant stops tests/journal.rs compiling (E0004: non-exhaustive patterns) and, once named, must also appear in ActionOutcome::ALL. every_outcome_keeps_its_wire_spelling pins each of the six literals rather than only their pairwise distinctness. Stated limit, unchanged: ALL is still a hand-written array and nothing inside the type derives it from the enum, which cannot be done here without a macro or a dependency; the pre-existing assert_eq!(ActionOutcome::ALL.len(), 6) is a compile-time tautology and is not what this row rests on."
    },
    {
      "name": "Idempotency exactness",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal idempotency_exactness && cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --doc journal",
      "result": "pass",
      "output": "Seven tests pass, re-measured unchanged at this base (7 passed, 0 failed), plus the journal doc suite at 7 ordinary and 8 compile-fail. Target half: replaying key-1 against the same target returns the byte-identical original receipt even when the replay supplies a different action id and outcome, and the same key against work/w-2 fails with JournalError::IdempotencyConflict { recorded_target: \"work/w-1\", offered_target: \"work/w-2\" } while the original recording stands. Payload half: same key, same target, payload sha256:aaa replays identically; payload sha256:bbb fails with JournalError::IdempotencyPayloadConflict and the recorded payload still reads sha256:aaa; a payload offered under a key first recorded without one fails the same way against ActionPayload::absent(); ActionPayload::digest(\"\") is refused with category 'field_invalid'. Narrowed claim, carried forward from the previous pass and re-verified in this one: the ActionLedger compile_fail pins that the name find_mut does not exist (E0599, checked standalone against the built rlib at this base), not that no mutable accessor can exist. Adding pub fn receipts_mut(&mut self) -> &mut [ActionReceipt] together with ActionReceipt::set_target still leaves the whole suite green — reproduced again in this pass, 39/39 integration and 7+8 doc tests passed with both methods present — so in-place overwrite is closed by the absence of an API rather than by type. The ActionLedger doc now also states why that residual is irreducible: both methods would have to be written into src/journal.rs itself, Rust's privacy boundary is the module, and no test can observe the absence of a method nobody has written."
    },
    {
      "name": "Receipt durability",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal receipt_durability && cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --doc journal && rustc --edition 2024 --extern automonique_protocol=rust/target/debug/deps/libautomonique_protocol-f9919e2c7dee2777.rlib <scratch copy of each compile_fail body>",
      "result": "pass",
      "output": "Three behavioural tests pass, re-measured unchanged at this base (3 passed, 0 failed). A receipt recorded inside a scope standing in for a client connection is still queryable by key after that scope ends, returning target work/w-1 and outcome Completed; the receipt clones, survives dropping the whole ActionLedger, and still reads idempotency_key and action_id; and a_lookup_answers_for_the_key_it_was_given asserts that find(\"key-2\") returns the key-2 receipt, find(\"key-1\") the key-1 receipt, and find(\"key-3\") None. The compile-fail pair carrying this row is ActionReceipt<'connection>: the positive half declares struct Session { receipt: ActionReceipt }, fills it from a ledger inside a scope and reads session.receipt.target() afterwards; the compile_fail half declares struct Session<'connection> { receipt: ActionReceipt<'connection> }. Re-verified standalone against the rlib built from this tree: the compile_fail body produces exactly one error, error[E0107]: struct takes 0 lifetime arguments but 1 lifetime argument was supplied. The other pre-existing compile_fail bodies were re-verified the same way at this base and each still produces exactly one error, the intended one: E0451 (field `pending` of struct `Outbox` is private), E0599 (no method named `find_mut` found for struct `ActionLedger`), E0616 (field `position` of struct `JournalCursor` is private)."
    },
    {
      "name": "Cursor monotonicity",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal cursor_monotonicity && cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --doc journal",
      "result": "fail",
      "output": "Ten tests pass (was seven; three are new) and the row is still recorded as not closed, for one position instead of for every position above the window. NOT a claim that the work is undone — see the last paragraph for why it is recorded fail.\n\nWHAT WAS MEASURED AS CLOSED. Unchanged and re-measured: advance_to(15) from 10 succeeds, advance_to(15) again is idempotent, advance_to(14) fails with JournalError::CursorWentBackwards { current: 15, offered: 14 } and position() stays 15; an empty consumer and an empty topic are refused; cursors differing only in consumer, or only in topic, are not equal; RetainedRange::new(400, 100) is refused with JournalError::InvertedRetainedRange { from: 400, to: 100 }; a cursor at 42 or 99 against 100..=400 yields CursorResume::ResyncRequired { snapshot_from: 100, snapshot_to: 400 } whose outcome() is Some(ActionOutcome::ResyncRequired) spelled 'resync_required'; 100, 250 and 400 yield CursorResume::Live { from }; a direct write to the private position field is compile_fail, re-verified standalone at this base as exactly error[E0616].\n\nNEW, AND A DISCLOSED BEHAVIOUR CHANGE. resume_within previously read 'outside the retained range' as 'below it', so EVERY position above the window resumed live: a cursor at 500 against 100..=400 returned Live { from: 500 }, which would begin delivery at a position the topic has not reached and silently skip everything later written between 401 and 500. That is the failure mode the CursorResume doc says is impossible ('never a silent partial stream'). resume_within now classifies three regions against the resumable window first ..= last+1, exposed as the new RetainedRange::caught_up(): below first resyncs, first..=caught_up is live, above caught_up resyncs. Observed: 402, 500, 10_000 and u64::MAX against 100..=400 now all return ResyncRequired { snapshot_from: 100, snapshot_to: 400 } with outcome 'resync_required'; 0, 1, 98, 99 resync; 100, 101, 250, 399, 400, 401 are Live { from: position }; 402 and 403 resync. caught_up() saturates, so a window of (u64::MAX-2)..=u64::MAX reports caught_up() == u64::MAX and positions u64::MAX-2, u64::MAX-1 and u64::MAX are all Live rather than wrapping to 0 and resyncing everything.\n\nNEGATIVE CONTROLS, each applied to src/journal.rs, run, then reverted, and the file confirmed back to its intended state by git diff afterwards. (1) Delete the upper bound (`if self.position < retained.first()`, the previous behaviour) -> 3 tests FAILED (a_cursor_ahead_of_the_topic_resyncs_rather_than_skipping_silently, the_resumable_window_is_first_through_caught_up_inclusive, the_caught_up_position_is_live_and_is_the_only_such_position), 7 passed. (2) Use `>=` instead of `>` so the caught-up position resyncs -> 3 tests FAILED (the_caught_up_position_is_live..., the_resumable_window..., a_window_ending_at_the_last_representable_position_does_not_overflow), 7 passed. (3) Replace saturating_add(1) with `self.last + 1` -> 1 test FAILED, a_window_ending_at_the_last_representable_position_does_not_overflow, panicking at src/journal.rs with 'attempt to add with overflow'; 9 passed. The three earlier mutations still hold and were not re-run in this pass: resume_within always returning Live, deleting the inversion check in RetainedRange::new, and CursorResume::outcome returning None for ResyncRequired.\n\nWHY THIS IS STILL RECORDED FAIL. The row as it stands at this candidate's base says 'an out-of-range cursor yields resync_required with snapshot coordinates', and the Journal contract bullet says 'A cursor outside the retained range'. Position 401 is outside 100..=400 and is still Live { from: 401 }, deliberately: a cursor names the boundary BEFORE the next position its consumer will receive, so a window first..=last has last-first+2 resumable cursor positions - one more than it retains - and the resumable set can never coincide with the retained set under this convention or the 'last acknowledged' one, which is off by one at the other end instead. Read literally the caught-up consumer resyncs, takes a snapshot covering through last, returns to last+1, and resyncs again: the literal reading livelocks the ordinary caught-up state. So the remaining gap is one position wide and closing it would be a defect. plan/contracts/R1-12.md now carries that as amendment A2 with named limit L2, quoting the superseded wording rather than replacing it. This candidate proposes that amendment and therefore may not be judged by it: AGENTS.md says a candidate is judged by the rules as they stood at its own admitted base, never by the version it introduces, so this row is answered against the superseded wording and answers 'fail'. What would close it: A2 landing on its own, judged by someone who did not write it; at that point the measurements above already satisfy it and no further code is needed.\n\nUNCHANGED AND OUT OF LEASE: event::resolve_subscription applies the old below-only rule to the parallel ConsumerCursor type in src/event.rs, so it still carries the above-window hole closed here, and journal::JournalCursor/RetainedRange duplicate event::ConsumerCursor/resolve_subscription. src/event.rs is outside this item's file lease; the divergence is now stated in the resume_within doc comment as well as here. Second and mechanical, recorded in full under notes: plan/gate.py --dry-run refuses this diff because plan/contracts/R1-12.md is not inside R1-12's allowed_paths, so the amendment cannot ride with this item at all and has to land as its own candidate."
    },
    {
      "name": "Replay purity",
      "command": "cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --test journal replay_purity && cargo test --manifest-path rust/Cargo.toml -p automonique-protocol --offline --doc journal && rustc --edition 2024 --extern automonique_protocol=rust/target/debug/deps/libautomonique_protocol-f9919e2c7dee2777.rlib <scratch copy of each compile_fail body>",
      "result": "fail",
      "output": "Five tests pass (was two; three are new) and eight compile-fail doc tests pass (was four; four are new). The row is still recorded fail, and the reason is that no implementation can satisfy it, not that the work was skipped.\n\nTHE ROW AS WRITTEN IS UNSATISFIABLE, RE-CONFIRMED BY MEASUREMENT. The row demands 'no effect, outbox, notification or transport handle is reachable inside replay'. A projection owns its fields, so anything it was constructed holding is reachable from apply. Measured here rather than argued: tests/journal.rs::replay_purity::a_projection_emits_during_replay_through_a_handle_it_brought builds an Emitter whose own field is an std::sync::mpsc::Sender<u64>, replays a three-event journal through it, and observes [1, 2, 3] on the receiving end - three effects that crossed the replay boundary, in the repository's own test suite, on every run. Rust has no effect system; 'the argument list contains no handle' and 'the callee performs no effect' are different claims and only the first is a type.\n\nWHAT IS NOW ENFORCED AND PINNED. Four properties, each a compile_fail paired with a passing case differing only in the rejected construct, and each compile_fail body compiled standalone against the rlib built from this tree to confirm it fails for the intended reason and produces exactly one error: (1) replay grants no second channel - journal.replay(&mut counter, &mut sink) is error[E0061]: this method takes 1 argument but 2 arguments were supplied, paired with journal.replay(&mut counter) which compiles; (2) the callback cannot be widened - fn apply(&mut self, _event: &DomainEvent, _outbox: &mut Outbox) is error[E0050]: method `apply` has 3 parameters but the declaration in trait `apply` has 2, paired with the two-parameter impl which compiles; (3) the callback gets no write access - fn apply(&mut self, _event: &mut DomainEvent) is error[E0053]: method `apply` has an incompatible type for trait ... types differ in mutability, paired with the same impl taking &DomainEvent; (4) no effect sink is obtainable from this crate - Outbox { pending: Vec::new() } is error[E0451]: field `pending` of struct `Outbox` is private, and Outbox::default() is error[E0599]: no function or associated item named `default` found for struct `Outbox`, the latter paired with Journal::default() which compiles. Behaviourally: replay shows the projection exactly the journal's own events, whole and in order (assert_eq!(recorder.shown.as_slice(), journal.events()), 3 of 3), and three replays of one journal are indistinguishable from each other and from journal.events().\n\nNEGATIVE CONTROLS, each applied to src/journal.rs, run, then reverted. (1) replay iterating self.events.iter().skip(1) -> all 5 replay_purity tests FAILED, 0 passed. (2) replay calling projection.apply(event) twice per event -> all 5 FAILED, 0 passed. (3) impl Default for Outbox added -> the Outbox::default() compile_fail FAILED (it now compiles) while the other 7 compile-fails and all 7 positive doc tests passed. (4) replay given a second parameter (&mut Vec<String>) -> exactly 2 doc tests FAILED: the two-argument compile_fail, because it now compiles, and its paired positive, because it no longer does. So both halves of that pair discriminate. No mutation exists that flips the E0050 or E0053 half alone: a trait method has one signature, so any mutation that admits the widened impl also breaks the paired positive - which is what 'differs only in the property under test' means for those two.\n\nWHY THIS IS STILL RECORDED FAIL. The enforceable boundary has been pushed as far as it goes and the residual is now written down as named limit L1 in plan/contracts/R1-12.md amendment A1, which quotes the superseded row and Journal-contract bullet rather than replacing them, states that no implementation can satisfy them, gives the wording that is enforceable, and records what would actually close L1 - an effect system the language does not have, or moving enforcement to a process or sandbox boundary that can deny a syscall. A1 also amends the stop condition 'any reachable effect inside replay', which as written is met by every projection that owns a handle. This candidate proposes A1 and therefore may not be judged by it (AGENTS.md: judged by the rules as they stood at its own admitted base, never by the version it introduces), so this row is answered against the superseded wording and answers 'fail'. What would close it: A1 landing on its own, judged by someone who did not write it.\n\nHONEST QUALIFICATIONS, kept rather than dropped. Nothing in the workspace constructs an Outbox (grep finds it only in journal.rs and its own doc tests), so the two Outbox compile-fails guard a declared boundary rather than a live delivery path, and the Outbox doc now says so. That no third route to an Outbox exists is read off the impl block, not pinned by a test. replay_is_repeatable_and_changes_nothing asserts journal == before, which cannot fail because replay takes &self and Journal has no interior mutability; it is kept as documentation of a type-level fact and the test now says so. every_replay_of_one_journal_shows_the_same_events would catch a replay that consulted a clock or counter to decide what to show; it cannot catch one that performed a constant effect on the way past, and says so. Second and mechanical, recorded in full under notes: plan/gate.py --dry-run refuses this diff because plan/contracts/R1-12.md is not inside R1-12's allowed_paths, so the amendment cannot ride with this item at all and has to land as its own candidate."
    },
    {
      "name": "Quality",
      "command": "cargo fmt --manifest-path rust/Cargo.toml --all -- --check && cargo clippy --manifest-path rust/Cargo.toml --workspace --all-targets --offline --locked -- -D warnings && cargo test --manifest-path rust/Cargo.toml --workspace --offline --no-fail-fast && python3 plan/check.py --verify",
      "result": "pass",
      "output": "All four exit 0, offline. Formatting is clean workspace-wide (cargo fmt --all -- --check exits 0 with no diff, including files owned by other agents). Strict all-target Clippy with --locked is clean across the workspace; --locked exiting 0 is also the measurement that no lockfile change is required by this work, which the previous pass could not make. Workspace tests: 58 suites, 1040 passed, 0 failed, 1 ignored - the 1028 recorded at the previous base plus this pass's 12 (integration tests in tests/journal.rs 33 -> 39, journal doc tests 5 ordinary + 4 compile-fail -> 7 + 8). automonique-protocol alone: tests/journal.rs 39/39, tests/protocols.rs 56/56, so the untouched public signatures still satisfy every caller. python3 plan/check.py --verify exits 0 with one expected warning about 5 frozen harness contracts. No allowance was added: grep for #[allow], todo!, unimplemented!, dbg! and unsafe over src/journal.rs and tests/journal.rs finds nothing, and the crate still declares zero dependencies. No Cargo.toml or Cargo.lock was edited; git status shows exactly three modified files, all inside this item's lease: rust/crates/automonique-protocol/src/journal.rs, rust/crates/automonique-protocol/tests/journal.rs and plan/contracts/R1-12.md.\n\nDISCLOSED, UNRESOLVED: one workspace run out of six during this pass reported 1038 passed / 2 failed. That invocation summed counts and did not capture the failing test names, which is a gap in my measurement. The five other full-workspace runs - four of them captured to files and inspected for the string FAILED - each reported 1040 passed / 0 failed, as did seven separate automonique-lab runs. It was not reproduced and is not attributed to anything; automonique-lab has several suites that spawn processes and read the clock (harness_claim and program_cli were recorded as intermittently failing at an earlier base for reasons outside this lease), which is a hypothesis and not a finding. Recorded here rather than dropped because a single unexplained failure is exactly the observation that gets lost. Re-measured after landing on 48682a2, a later base than the one this agent worked from: it carries the automonique-lab integration-ceiling repair, the regenerated .automonique/dev/program.yaml, the six sibling R1 items landed alongside this one, and the plan/check.py scan-scope fix. At that base: fmt exits 0; clippy --workspace --all-targets --offline --locked -D warnings exits 0 with zero warnings; cargo test --workspace --offline --no-fail-fast is 58 suites, 1139 passed, 0 failed, with zero GAP lines, so nothing silently returned for a missing runtime; plan/check.py --verify exits 0 with 0 failures; rust/Cargo.lock and every Cargo.toml are unmodified and the protocol crate still declares no dependencies."
    }
  ],
  "review": {
    "reviewers": 0,
    "blocking_findings": 0,
    "acceptance": "no independent reviewer examined this item. The findings acted on in this pass come from an automated adversarial agent in a separate session, recorded under adversarial_review with independent: false; that is a second machine reading the same diff, not independent review of the design, so the reviewer count stays zero. Every mutation claimed closed in this pass was applied to the working tree, observed to fail the suite it should, and reverted, and the two defects that could not be closed were reproduced before being recorded as open. The two contract amendments this candidate proposes are unreviewed by anyone, which is precisely why the two rows they concern are recorded fail rather than pass."
  },
  "notes": "This pass changed two lines of behaviour and added tests, documentation and a contract amendment around them.\n\nBEHAVIOUR CHANGED, DISCLOSED RATHER THAN FOLDED IN: JournalCursor::resume_within now resyncs a cursor above the resumable window instead of resuming it live, and RetainedRange::caught_up() is a new public accessor for that window's upper bound. Nothing else in src/journal.rs changed except doc comments. No public signature was removed or narrowed, so protocols.rs::commit and every other caller are unaffected and tests/protocols.rs still passes 56/56. The old behaviour served a cursor ahead of a truncated or rebuilt topic with live delivery starting at a position the topic had not reached, which silently skips everything later written below that position; that is a real defect and fixing it is why this is a behaviour change rather than a documentation pass.\n\nTests: tests/journal.rs 33 -> 39. Cursor: the_caught_up_position_is_live_and_is_the_only_such_position (replacing a_caught_up_cursor_is_live_rather_than_a_resync, which it contains), a_cursor_ahead_of_the_topic_resyncs_rather_than_skipping_silently, the_resumable_window_is_first_through_caught_up_inclusive, a_window_ending_at_the_last_representable_position_does_not_overflow. Replay: replay_shows_the_projection_the_journals_own_events_and_nothing_else, every_replay_of_one_journal_shows_the_same_events, a_projection_emits_during_replay_through_a_handle_it_brought. Doc tests: 5+4 -> 7+8, four new compile-fails (E0061, E0050, E0053, E0599) each paired with a passing case differing only in the rejected construct, each compiled standalone against the built rlib and each producing exactly one error, the intended one.\n\nMethod, unchanged from the previous pass: for every test that claims to pin something, apply the mutation that removes the behaviour, watch the suite fail, revert, watch it pass. Nine mutations were run this way in this pass (three on resume_within/caught_up, two on replay, one adding Default for Outbox, one adding a second replay parameter, and the two reproductions of D5a and D5b below). Unlike the previous pass these were applied to the working tree of this isolated worktree rather than to a scratch copy, and git diff after each revert confirms only the two intended code lines differ from base.\n\nCONTRACT AMENDED, AND DELIBERATELY NOT USED TO CLOSE ANYTHING. plan/contracts/R1-12.md gains an '## Amendments' section holding A1 (replay purity) and A2 (cursor monotonicity), each quoting the superseded row and Journal-contract bullet verbatim, saying what cannot be met and why, giving the enforceable wording, and naming the residual it leaves as a numbered limit: L1 projection-owned handles, L2 the caught-up position. The rows and bullets above are left as they stood, with a pointer added, so the rule that judges this candidate is unchanged and visible. AGENTS.md forbids a candidate being judged by rules it introduces, so both rows are answered against the superseded wording and both answer fail. That is the whole reason two rows that have working, mutation-checked implementations behind them are not marked pass: an implementer may not certify itself by amending the check, and BOOT-001's evidence set the precedent for that in this repository.\n\nSTILL OPEN, reproduced again rather than argued away. D5a: adding pub fn receipts_mut(&mut self) -> &mut [ActionReceipt] together with ActionReceipt::set_target leaves the suite green (measured in this pass: 39/39 integration, 7+8 doc). D5b: adding pub const fn set_position(&mut self, u64) to JournalCursor leaves the suite green (same run). Both are irreducible for the reason now written into the ActionLedger and JournalCursor docs: both mutations have to be written into src/journal.rs itself, Rust's privacy boundary is the module and no module can be protected from code added to it, and no test can observe the absence of a method nobody has written. Outside the module both properties hold by type and the compile-fails pin that boundary. Closing them would need an API-surface snapshot tool, which means a dependency this crate forbids. One structural narrowing was considered and rejected: moving JournalCursor and ActionLedger into nested private modules inside journal.rs would make both mutations fail with E0616 unless written inside those modules, which shrinks the trusted region without closing the hole, at the cost of a few hundred lines of reindentation in a diff whose reviewability is the actual control here. Recorded as a considered option rather than silently skipped.\n\nAlso left alone deliberately: AggregateId::new and DomainEvent::new's bounded(kind) checks are unpinned by any test, but no contract row demands them; and table-level transactionality remains deferred to R4 by the contract itself.\n\nThe base sha is this worktree's HEAD, 4b491316751b2e489e10d6d5b495dbab7948612e, read with git rev-parse HEAD; it has moved since the previous measurement because sibling sessions committed in between.\nGATE PREFLIGHT, RUN AND RECORDED RATHER THAN SUMMARISED. `python3 plan/gate.py --dry-run --item R1-12 --summary ... --files <the four files above>` returns FAIL with four refusals, and every one of them is expected and correct at this base:\n\n  REFUSE:  check 'Cursor monotonicity' failed\n  REFUSE:  check 'Replay purity' failed\n  REFUSE:  R1-12 leased ['rust/crates/automonique-protocol/', 'rust/crates/automonique-policy/', 'rust/Cargo.toml', 'sdk/typescript/packages/protocol/', 'xtask/'] but the diff touches: plan/contracts/R1-12.md\n  REFUSE:  no specification-debt counter decreased.\n\nThe first two are the two rows this pass could not honestly close. The fourth is ordinary for an item that implements an existing contract (AGENTS.md provides --allow-no-metric-change for exactly this).\n\nThe third is a real finding and it changes how this patch must land: plan/contracts/R1-12.md is NOT inside R1-12's allowed_paths in plan/work-graph.toml, so the contract amendment cannot ride along with the implementation. That is the same rule AGENTS.md states in prose - a completion transaction 'cannot touch plan/authority.toml, its own contract, this file or the gate, so a candidate can never certify itself by widening the rule that judges it' - and it is a second, mechanical reason why the Cursor monotonicity and Replay purity rows are recorded fail here rather than pass. The amendment was written because the dispatching instruction asked for it and granted the file; it is honest work and it is in the patch, but it is a separate work unit under AGENTS.md's contract-preparation exception and has to be committed and judged as its own candidate, by someone who did not write it. Splitting the patch on that line, rather than reverting the amendment, is what this agent recommends; reverting it would drop the two named limits L1 and L2 back into prose that nobody is accountable for, which is the failure this repository exists to prevent.\n",
  "adversarial_review": {
    "reviewer": "automated adversarial agent, separate session from the implementer",
    "independent": false,
    "method": "copied the crate, applied behavioural mutations, re-ran the suite to find rows whose tests survive the mutation",
    "caveat": "Disposition after the repair pass that followed this review. Every mutation below was reproduced on a scratch copy before anything was changed, so no finding is recorded as closed on argument alone.\n\nCLOSED: D1 (seventh ActionOutcome variant absent from ALL) - killed by tests/journal.rs::outcome_vocabulary::every_variant_of_the_outcome_enum_is_present_in_all plus the exhaustive wire_spelling match, under which the mutation stops the test target compiling (E0004); the residual limit that ALL is still hand-written is stated on the row. D2 (five of six wire spellings rewritten) - killed by every_outcome_keeps_its_wire_spelling, 1 test FAILED. D3 (ActionLedger::find ignoring its key) - killed by receipt_durability::a_lookup_answers_for_the_key_it_was_given, 1 test FAILED. D4 (receipt-durability compile_fail a tautology) - the tautology was reproduced both with std types only and by a mutation that makes a borrowing receipt type representable while the old compile_fail kept passing; the compile_fail was replaced by an ActionReceipt<'connection> pair whose two halves both fail under that mutation (2 doc tests FAILED) and which produces exactly one error, E0107, standalone. D8 (record() doc calling a retarget conflict a second execution) - doc corrected to the contract's wording.\n\nSTILL OPEN, recorded rather than closed: D5a and D5b - a mutable accessor under a different name (receipts_mut plus a receipt setter) and an unchecked cursor set_position both leave the suite green (re-measured: 33/33 integration, 5+4 doc). No test can pin the absence of a method that does not exist yet, so the two doc comments were narrowed to claim only what the compile-fails pin. D6 (cursor 'outside the retained range' read as 'below it') - not closed; the Cursor monotonicity row is now recorded fail, the narrowing is stated in the code doc and on the test, and reconciling the contract sentence with the implementation is left as an owner decision. D7 (replay purity overclaim) - the counterexample was reproduced by compiling and running a projection that writes a file and sends on a channel during replay; the claim cannot be enforced in safe Rust, so the module and Outbox docs were corrected and the Replay purity row is now recorded fail.\n\nThe original meaning of this caveat still stands for any row not listed above: a 'pass' means the row was measured and observed to hold, not that every sentence of the contract behind it is pinned by a test that would fail if the behaviour were removed.\n\n--- DISPOSITION AFTER THE SECOND REPAIR PASS (base 4b491316751b2e489e10d6d5b495dbab7948612e, this session). D1-D4 and D8 unchanged, re-measured green. D5a and D5b: still open, reproduced again at this base (both mutations leave 39/39 integration and 7+8 doc tests green); now argued to be irreducible rather than merely unclosed, because both mutations must be written into src/journal.rs itself and module privacy cannot defend a module against itself - the two doc comments say exactly that, and the rejected structural narrowing (nested private modules) is recorded in the notes. D6: the half of it that was a real hole is closed - every position above the retained window used to resume live and now resyncs, with three negative controls - leaving a one-position gap at last+1 that is forced by cursors naming boundaries rather than events; contract amendment A2 states it and names it L2, and the row stays fail because this candidate may not be judged by its own amendment. D7: the enforceable part is now proven by four paired compile-fails (E0061, E0050, E0053, E0599) on top of the existing E0451, all verified standalone, and the unenforceable part is demonstrated by a passing test that emits three effects through a projection-owned channel during replay; contract amendment A1 states the enforceable property and names L1, and the row stays fail for the same reason. Both rows are therefore 'implemented and measured, awaiting an amendment that someone other than the implementer may judge', which is a different state from the one this review found, where the rows were marked pass and the claims were not measured at all.",
    "findings": "No repo files were modified. All mutations were run against a copy of the crate in the scratchpad (`/tmp/claude-1025/-home-infra-sj278-automonique/189dce1c-a957-463e-bb89-3fdb77b0d452/scratchpad/proto`), built standalone with `rustc`/`rustdoc` (the crate has zero dependencies), and reverted after each run. Repo baseline re-verified: 30 integration tests + 4 doc tests + 4 compile-fail doc tests pass.\n\n# Verdict per contract row\n\n| Row | Genuinely closed? |\n|---|---|\n| Global ordering | Yes |\n| Revision algebra | Yes |\n| Schema independence | Yes |\n| Outcome vocabulary | **No** — marked `pass`, measures almost nothing (D1, D2) |\n| Idempotency exactness | Yes for the payload clause the implementer added; supporting assertions are hollow (D3, D5a) |\n| Receipt durability | Yes, but only via the positive half; the compile-fail the implementer added is a tautology (D4) and `find` is unpinned (D3) |\n| Cursor monotonicity | Yes for monotonicity and below-window resync; the \"outside the retained range\" clause is half-implemented (D6, disclosed) |\n| Replay purity | **No** — marked `pass`; the doc and the evidence claim an enforcement the code does not perform (D7) |\n| Quality | Honestly recorded as `fail`; supporting claims verified true |\n\n# What the implementer got right\n\nEvery mutation the evidence file claims to have run reproduces exactly, with the exact failure counts stated:\n\n- delete payload comparison in `record_with_payload` (journal.rs:752) → 2 tests fail\n- remove `bounded()` from `ActionPayload::digest` (journal.rs:537) → 1 test fails\n- `resume_within` always returns `Live` (journal.rs:935) → 2 tests fail\n- delete inversion check in `RetainedRange::new` (journal.rs:805) → 1 test fails\n- `CursorResume::outcome` returns `None` for `ResyncRequired` (journal.rs:856) → 1 test fails\n- make `JournalCursor.position` public → cursor compile-fail flips to FAILED\n- add a real `find_mut` to `ActionLedger` → ledger compile-fail flips to FAILED\n- make `Outbox.pending` public → module compile-fail flips to FAILED\n\nEach of the four `compile_fail` bodies, compiled standalone against the rlib, produces exactly one error and it is the intended one: `E0597` (receipt), `E0616` (cursor), `E0451` (outbox), `E0599 no method named find_mut` (ledger). Each is paired with a passing twin that differs only in the targeted construct. No unrelated-typo failures. The additive-change claim holds: `protocols.rs:1596 commit` still uses the untouched 5-argument `record`, and `scoped_idempotency_key` folds `action_id` into the key so no payload-conflict path is bypassed there. `base` `34c60053…` matches `.git/refs/heads/main` byte for byte. `automonique-lab/Cargo.toml` declares no `automonique-protocol` dependency, so the Quality-row attribution is correct. No `#[allow]`, `todo!`, `unimplemented!`, `dbg!` or `unsafe` in either owned file.\n\n# Defects\n\n**D1 — `journal.rs:480` / `tests/journal.rs:196-206`: the \"closed and exhaustive\" outcome set is not pinned. Row marked `pass`.**\nSurviving mutation: add a seventh variant `Superseded` to `ActionOutcome` with an `as_str` arm and *do not* add it to `ALL`. Result: 30/30 integration + 8/8 doc tests still pass. `ActionOutcome::ALL` is a hand-maintained mirror (`pub const ALL: [Self; 6]`) with nothing tying it to the enum, so the vocabulary can be widened silently — which is a listed stop condition (\"an open-ended outcome vocabulary\"). Compounding this, `assert_eq!(ActionOutcome::ALL.len(), 6)` at tests/journal.rs:197 is a compile-time tautology: `ALL` is typed `[Self; 6]`, so that assertion can never fail. The evidence text \"the enum has no catch-all variant, so the set is closed\" is an inspection result, not something the named command measured.\n\n**D2 — `journal.rs:491-500`: no test pins any wire spelling except `resync_required`.**\nSurviving mutation: rewrite five of the six spellings (`\"accepted\"`→`\"ACCEPTED\"`, `\"conflict\"`→`\"CONFLICT-X\"`, etc.). Result: 30/30 + 8/8 still pass, because tests/journal.rs:198-205 only checks that the six are pairwise distinct. The evidence output for this row quotes \"6 distinct wire spellings (accepted, completed, rejected, conflict, unknown, resync_required)\" as if the command measured those literals; it did not. This is doctrine (d): a check whose command could not have detected the failure it reports as absent.\n\n**D3 — `journal.rs:778-782`: `ActionLedger::find` ignores nothing today, but no test would notice if it did.**\nSurviving mutation:\n```rust\npub fn find(&self, _idempotency_key: &str) -> Option<&ActionReceipt> {\n    self.receipts.first()\n}\n```\nResult: 30/30 + 8/8 still pass. No test ever calls `find` with a key that is not the first recorded receipt, and no test calls it with an absent key. This hollows out the \"the original stands\" assertions at tests/journal.rs:274-277 and 352-359 (Idempotency exactness) and both Receipt-durability tests at tests/journal.rs:412 and the `ActionLedger` positive doc test at journal.rs:646-654 — every one of them would pass against a lookup that returns an arbitrary receipt. Missing: an assertion that `find(\"key-2\")` returns the key-2 receipt and `find(\"absent\")` returns `None`.\n\n**D4 — `journal.rs:582-593`: the receipt-durability `compile_fail` is a tautology of the borrow checker, not a measurement of the contract property.**\nThe body fails with `E0597 connection does not live long enough`, which is true of *any* borrow of *any* dropped local. Demonstration: the identical shape with std types only —\n```rust\nlet receipt: &String;\n{ let mut connection: Vec<String> = Vec::new();\n  connection.push(\"work/w-1\".to_owned());\n  receipt = connection.first().unwrap(); }\nassert_eq!(receipt, \"work/w-1\");\n```\n— produces the same single `E0597`, with nothing from journal.rs involved. The evidence itself discloses the counter-mutation (making `find` return owned did not flip it, because the error merely becomes `E0308`), which is honest, but the conclusion drawn — that the row's compile-fail clause is now implemented — overstates it. The contract property (\"a receipt type borrowing from a live connection is unrepresentable\") is in fact carried entirely by the *positive* half (journal.rs:565-576) plus tests/journal.rs:425-434; the compile-fail half adds no discriminating power. Note the evidence's phrasing for this row — \"the compile-fail clause of this row was previously unimplemented … it now exists\" — is the weakest claim in the file.\n\n**D5 — the compile-fail guards are name-specific, so the \"closed by type\" claims are narrower than stated.** Two surviving mutations that destroy the property while every compile-fail keeps passing:\n- **D5a** (journal.rs:661-669, evidence: \"Overwrite is additionally closed by type\"): add `pub fn receipts_mut(&mut self) -> &mut [ActionReceipt]` plus `ActionReceipt::set_target`. A recorded receipt can now be retargeted in place. 30/30 + 8/8 still pass, including the `find_mut` compile-fail, which only proves that one method *name* is absent.\n- **D5b** (journal.rs:877-882, doc at journal.rs:883 claims \"there is no path around that check and a rewind cannot be performed without producing the typed error\"): add `pub const fn set_position(&mut self, position: u64)`. Unchecked rewind is now reachable; 30/30 + 8/8 still pass because the field is still private. The doc sentence is true of today's code but the guard only covers the direct-field-write route.\n\n**D6 — `journal.rs:935-946` / `tests/journal.rs:523-532`: the Cursor-monotonicity row is closed against a narrowed reading of the contract.**\nContract line 53: \"A cursor outside the retained range yields `resync_required` with bounded snapshot coordinates.\" `resume_within` treats only `position < retained.first()` as outside; a cursor at 401 against `100..=400` is literally outside the range and returns `CursorResume::Live { from: 401 }`. tests/journal.rs:523 (`a_caught_up_cursor_is_live_rather_than_a_resync`) actively encodes the opposite of the contract sentence. The implementer disclosed this in the evidence in plain terms and the reading is defensible (`position` is documented as the *next* position to be received), and I verified the consistency claim: `event.rs:702-717 resolve_subscription` uses the identical rule. But a disclosed narrowing of contract text is an owner decision, and the row is nonetheless recorded `pass` rather than as a partial. Per doctrine 8 this is the case for a `null`-with-reason or an explicit recorded decision, not a `pass`.\n\n**D7 — `journal.rs:5-8` and `journal.rs:359-368`: doc comment (and the evidence) claim an enforcement the code does not perform. Row marked `pass`.**\nModule doc: \"It may never drain an outbox or repeat an effect, and that is enforced by reachability rather than by convention\". `Outbox` doc: \"A `Projection` therefore cannot obtain one, so replay cannot emit.\" Evidence: \"no effect, outbox, notification or transport handle is reachable inside replay\". Counterexample, compiled against the built rlib and executed:\n```rust\nstruct Emitter { sink: std::sync::mpsc::Sender<String> }\nimpl Projection for Emitter {\n    fn apply(&mut self, event: &DomainEvent) {\n        std::fs::write(\"/tmp/claude-1025/replay-effect.txt\", event.kind()).unwrap();\n        self.sink.send(format!(\"emitted {}\", event.event_id())).unwrap();\n    }\n}\n```\nOutput: `replay emitted: Ok(\"emitted 1\")` / `file written: Ok(\"created\")`. A projection owns its own fields, so any transport, channel or filesystem handle it closes over is reachable from inside `Journal::replay`. The reachability argument covers exactly one type, `Outbox`, which no code anywhere in the workspace can construct (grep confirms it is referenced only inside journal.rs and its own doc test — `automation.rs`'s `OutboxObligation` is unrelated). The compile-fail is therefore a guard on a decoy. The stop condition \"any reachable effect inside replay\" is met on the literal reading. The honest form of this row is `pass` on \"the crate hands out no effect handle\" with a stated limit that Rust cannot make a callback pure, not the unqualified claim now recorded.\n\n**D8 — `journal.rs:692-694`: doc wording states the opposite of the contract.**\n`record`'s `# Errors` reads \"Returns `JournalError::IdempotencyConflict` when a key is reused for a different target — a second execution, not an overwrite —\". Contract lines 46-48 say the retarget case is \"a typed conflict, **not** an overwrite **and not** a second execution\", and `record_with_payload`'s own doc at journal.rs:719-721 states it correctly. As written, `record` labels the conflict as a second execution, which is the exact confusion the row exists to prevent. Cosmetic in effect, but it is the shared-API doc a caller reads.\n\n# Non-defects worth naming\n\n- `tests/journal.rs:572-584 replay_is_repeatable_and_changes_nothing`: `assert_eq!(journal, before)` cannot fail, because `replay` takes `&self` and `Journal` has no interior mutability. Unfalsifiable rather than wrong; the type already guarantees it.\n- `AggregateId::new` (journal.rs:72) and `DomainEvent::new`'s `bounded(kind)` (journal.rs:284) are unpinned — deleting both bounds checks leaves 30/30 + 8/8 passing — but no contract row demands them.\n- The duplication between `journal::JournalCursor`/`RetainedRange` and `event::ConsumerCursor`/`resolve_subscription` is declared in the evidence notes as an out-of-lease finding rather than papered over. That is the correct handling.\n\n# Summary\n\nNothing was silenced, no test was deleted or weakened, no allowance was added, no shared signature was broken, and the two rows the implementer moved fail→pass have real code behind them with mutation coverage that reproduces exactly as claimed. The failures are in rows the evidence declared \"re-measured unchanged\" and waved through: **Outcome vocabulary is measured by two tests that cannot fail (D1, D2), and Replay purity is recorded as proven when a three-line counterexample defeats it (D7).** Both are marked `pass` and both are measurable as failing. D3 and D4 are weaker than their evidence text asserts but do not by themselves make their rows false."
  }
}
