# Behavioural contract inventory — measured coverage

GENERATED by `tools/contract_inventory/build.py` — do not edit by hand.
Regenerate with `python3 tools/contract_inventory/build.py`; verify with
`python3 tools/contract_inventory/check.py`.

Every number below was counted from the permitted sources listed in
`inventory.json`. Nothing here is estimated, and an entry the sources do
not determine is reported unowned or as a gap rather than filled in.

- entries: **195**
- unowned entries: **12**
- blocked fixture plans: **82**
- unclassified findings: **3**
- gaps: **9**

## Entries per surface class

| Surface class | Entries |
|---|---:|
| input | 62 |
| state transition | 42 |
| timer | 8 |
| side effect | 34 |
| command | 21 |
| backend event | 12 |
| operational script | 16 |

## Entries per family

| Family | Entries |
|---|---:|
| `chat-platform-method` | 13 |
| `command` | 19 |
| `companion` | 7 |
| `conversation-route` | 13 |
| `dashboard-route` | 39 |
| `durable-table` | 16 |
| `operational-script` | 9 |
| `outbound-host` | 8 |
| `parity-row` | 38 |
| `provider-dialect` | 4 |
| `spool-event-kind` | 5 |
| `status-literal` | 16 |
| `timer` | 8 |

## Entries per target owner

| Target owner | Entries |
|---|---:|
| `automonique-agents` | 6 |
| `automonique-bootstrap` | 1 |
| `automonique-core` | 67 |
| `automonique-fleet` | 5 |
| `automonique-policy` | 1 |
| `automonique-runner` | 7 |
| `automonique-shell` | 5 |
| `automonique-store` | 35 |
| `automonique-transports` | 18 |
| `automonique-web` | 38 |
| _unowned_ | 12 |

## Fixture plans

A plan is blocked when it would need a secret, live network access, a
production read or a production write. Nothing in the corpus index is a
sanitized capture, so every entry's corpus status is `absent`.

| Fixture class | Entries |
|---|---:|
| `enumeration-presence` | 95 |
| `schema-snapshot` | 16 |
| `legacy-test-conversion` | 20 |
| `replay-capture` | 5 |
| `provider-transcript` | 5 |
| `effect-recording` | 36 |
| `none-replaced` | 18 |

## Unowned entries

The porting map does not determine a destination for these. Each one is a
finding for the parity-ledger item to resolve, not an omission.

- `companion:a guard binary` — the same porting map row names runner/broker crates in prose and no automonique- destination, so the guard binary's owner is not determined
- `companion:a sandbox (with C source)` — the porting map row for the sandbox C source and the privileged broker names runner/broker crates in prose and no automonique- destination, so the sandbox's owner is not determined
- `operational-script:benchmark` — no porting map row names a benchmark destination, and the permitted sources do not say what it measures
- `operational-script:build scripts` — no porting map row names a destination for the legacy build scripts; the Rust workspace's build jobs are new work rather than a port
- `operational-script:deploy-broker installer` — the porting map row for the privileged broker names runner/broker crates in prose and no automonique- destination, so the installer's owner is not determined
- `outbound-host:loopback address` — a loopback destination is a deployment detail; the porting map names no destination that owns it, and which process listens on it is not stated in a permitted source
- `parity-row:announce-target-before-action-and-show-site-server-ip` — safety row: announce-before-mutate spans work events, which legacy-inventory.md lists in the genuinely-new column, and the Slack outbox; no porting-map row determines the owner
- `parity-row:codex-worker-guard-hooks-and-sandbox-launcher` — the porting map row for the sandbox and privileged broker names runner/broker crates in prose and no automonique- destination, so the sandbox launcher's owner is not determined
- `parity-row:dedicated-deployment-notifications-channel` — safety row: the ledger names a typed deploy webhook and outbox; the dashboard row assigns the webhook route to automonique-web while the fail-closed channel policy is a transport concern, and no row determines which owns it
- `parity-row:detailed-github-report-plus-brief-slack-completion-link-mentions` — no porting-map row names report publication; the ledger's report artifact plus outboxes spans the typed artifact service, which legacy-inventory.md lists in the genuinely-new column, and the transport outbox
- `parity-row:human-work-approval` — the ledger names an approval repository and a policy; the porting map row that covers durable records lists automonique-store, automonique-core and automonique-policy without choosing between them, and no other row covers approvals
- `parity-row:learned-domain-to-server-targets` — the ledger assigns learned targets to the workspace registry, which legacy-inventory.md lists in the genuinely-new column; the porting map names no destination for it

## Unclassified findings

Observations that fit none of the seven classes. They are listed rather
than absorbed into the nearest class.

- `companion:a knowledge-base directory` — a directory of site-platform knowledge-base files is versioned content, not behaviour; it is not an input, state transition, timer, side effect, command, backend event or operational script
- `configuration-surface` — a configuration name is neither behaviour nor an operational script; it parameterises behaviour that is inventoried elsewhere, and forcing it into a behavioural class would double-count the behaviour it configures
- `parity-row:site-platform-knowledge-base-files` — a versioned content bundle carrying a release hash and workspace applicability metadata is a packaging contract; it is not an input, state transition, timer, side effect, command, backend event or operational script

## Gaps

What the permitted sources do not contain, and what would close each one.

| Gap | Kind | What would close it |
|---|---|---|
| `behavioural-coverage-unattributed` | unnamed-member | a per-file test listing from the legacy tree, which is a permitted structural reference but is not checked in here |
| `build-scripts-not-individually-named` | unnamed-member | a file listing of the legacy tree's script directory, which is a structural reference and permitted, but is not present in this repository |
| `command-count-discrepancy` | count-discrepancy | a structural reference from the legacy tree naming the counted members — permitted by the clean-room boundary, but not present in this repository |
| `dashboard-route-count-discrepancy` | count-discrepancy | a reconciliation of the stated count with the enumeration in the source document, which is a documentation fix rather than a capture |
| `durable-table-columns-unavailable` | unreachable-source | a read-only schema capture of the live database, which the fixture-capture gate blocks until the oracle boundary exists |
| `environment-variable-names-deferred` | deferred-to-item | the identifier inventory, which classifies every configuration name as durable, compatibility-only or presentation-only |
| `module-destinations-incomplete` | unnamed-member | a file listing of the legacy source tree, which is a permitted structural reference but is not checked in here |
| `no-sanitized-fixture-corpus` | unreachable-source | the sanitized fixture corpus item, which is itself blocked by the oracle gate |
| `provider-event-vocabularies-not-enumerated` | unreachable-source | a checked-in provider compatibility matrix and sanitized transcripts per native and fallback mode |

## Cross-source consistency

Two documents count the same rows; the checker fails if they stop agreeing.

| Measurement | Value |
|---|---:|
| parity_ledger_rows | 39 |
| coverage_map_pinned | 15 |
| coverage_map_partial | 5 |
| coverage_map_unpinned | 19 |
| coverage_map_total | 39 |

## Scope

This inventory is evidence for the sanitized fixture corpus item and the
machine-readable parity ledger item. It satisfies neither: it records what
exists and where it is going, captures no fixture and closes no gate.
