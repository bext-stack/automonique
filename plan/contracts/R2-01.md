# R2-01 — RunSpec schema and validator

| | |
|---|---|
| Epic | `R2` — execution substrate |
| Track | core |
| Depends on | `R1-25` |
| Immutable preparation base | `38497d440b245e7c9721a8b497e1211aa3254142` |
| Licence class | `Elastic-2.0` |
| Implementation allowlist | `rust/crates/automonique-runner/`, `rust/crates/automonique-protocol/`, `rust/Cargo.lock` |
| Scope budget | two existing crates, one existing local dependency edge, zero new registry dependencies and zero launch authority; each admitted session keeps the generated three-iteration/30-minute budget and may publish only a truthful partial until all checks pass on one completion tree |
| Hill-climbability | 94 — every field, bound, cross-field invariant and canonical byte is directly testable; later use-time containment remains explicitly outside this slice |

## Objective

Evolve the existing `automonique_runner::RunSpec` in place into the sole
immutable, versioned execution-admission document. It composes canonical types
already owned by `automonique-protocol` wherever their semantics match; narrow
protocol edits may add missing read-only accessors and canonical projections
but may not widen authority. It does not introduce a parallel `RunSpecV2`.
Concepts that have no matching protocol type are runner-private, bounded and
domain-tagged rather than plain caller strings: workspace-registry ID,
variant-specific protected prompt references, I/O and workspace reservations, profile/persona/
skill/extension/execution-plan digests, scheduler-reservation binding,
integration mode/fallback eligibility, portability/remote-attestation policy,
artifact-grant binding, credential audience/version binding, runner event dialect and
durable origin binding. Scheduler reservations and artifact grants are
digest-bound references that claim authoritative service records: a caller may
carry their coordinates but cannot prove issuance, mint scheduler capacity or
obtain artifact access by constructing a `RunSpec`.

The value binds what an admitted attempt is allowed to request before any
protected material is handed off. It contains no prompt bytes, credential
values or ambient authority. A valid value remains non-executable: the current
runner containment refusal must stay in force until later work installs and
attests a descendant-complete boundary.

## Admission contract

- The document has one closed schema identifier, `automonique.run-spec/v1`.
  Construction and decoding both pass through the same validator. Public
  fields remain private, mutation has no public route, and all rejections carry
  a stable typed category. Successful decoding proves only that the immutable
  declarative request is well formed and internally consistent; it grants no
  launch, credential, artifact, scheduler, transport or other use-time
  authority.
- The existing `RunSpec` and `RunSpec::new(RunSpecParts)` names are preserved.
  There is no second run-spec type and no duplicate representation of protocol
  concepts.
- The spec binds the work, run, attempt, host and host-lifetime coordinates;
  execution backend and portability policy; a runner-private typed workspace
  registry ID plus the canonical `WorkspaceRegistration` (tenant, source,
  revision, immutable snapshot, isolation and token); the complete canonical
  `SandboxSpec`; provider mode, fallback and required
  capabilities; session or resume coordinates; context/profile/model/tool,
  skill and extension digests; durable origin and causation; executor,
  executable and protocol digests; artifact and credential descriptors; and
  the output dialect. The nested `SandboxSpec` remains the only run-level
  sandbox and approval-policy binding.
- `SandboxSpec` is the single authority for its actor/tenant/provider-account,
  workspace context and base, sandbox/approval policy, path and mount grants,
  provider/tool egress, credentials, budgets, required enforcement features,
  nested isolation and prohibited-capability fields. `RunSpec` does not copy
  them. The validator compares only independently necessary execution
  coordinates — workspace registration tenant/base/isolation and the supported
  nested timeout/spool envelopes — against the authoritative nested values. Missing public
  projections needed for that comparison or for canonical encoding are added
  as read-only protocol accessors in this slice. Workspace
  isolation maps explicitly to the required read-only/write policy.
- Prompt delivery is metadata only: a typed protected-channel/reference
  coordinate names the later R2-02 handoff. `stdin` carries mode only and no
  reference; protected-file and backend-session modes carry distinct,
  non-interchangeable domain-tagged descriptor coordinates, never a filesystem
  path. Prompt bytes never occur in the spec,
  canonical bytes, `Debug`, display text or errors. Credential fields
  contain descriptors, audience and version coordinates only, never secret
  material.
- Paths and platform byte strings have one lossless canonical representation.
  Absolute paths containing empty, dot, dot-dot or NUL components refuse.
  Argument and environment bounds cover item, count and aggregate bytes;
  duplicate environment keys refuse.
- Every duration, byte quota, CPU, memory, PID, I/O, temporary, workspace and
  artifact budget has a public finite bound and checked arithmetic. Zero is
  accepted only where its documented meaning is `disabled`.
- Cross-field validation is limited to declared shared semantics: workspace
  tenant/base/isolation, the supported timeout and spool envelope, any present
  session binding's tenant/backend/provider account, a backend-session prompt's
  exact session, selected-mode exclusion from its fallback set, noninteractive
  cause actor/run, context budget, executor/attestation compatibility and the
  exact sandbox credential-name set. Provider capabilities and OS sandbox
  prohibitions remain separate namespaces. A comparison is never invented
  merely because two fields both contain a string or revision.
- `IntegrationMode` is an opaque, bounded provider-declared coordinate, not a
  global vocabulary shared by all providers. Required provider capabilities
  are later satisfied by the pinned observed mode declaration/selection;
  sandbox prohibited capabilities remain independently enforced OS names.
- Every spec carries exactly one `SchedulerReservationBinding` containing only
  a typed reservation ID, nonzero revision and canonical SHA-256 decision
  digest that names a scheduler record. A spec carries zero to 128
  `ArtifactGrantBinding` values, each containing only a distinct typed grant
  ID, nonzero revision and canonical SHA-256 grant digest that names an
  artifact-service record. These references convey no authority until later use-time code
  reopens the authoritative record and verifies subject, scope, direction,
  quota, expiry and current state. The sum of verified live grant quotas must
  fit the nested sandbox artifact budget before use. The RunSpec never embeds
  an output digest for bytes that do not exist yet.
- Reservation and grant IDs are distinct opaque ASCII coordinates matching
  `[A-Za-z0-9][A-Za-z0-9._-]{0,255}` while refusing the complete values `.` and
  `..`. Their revisions use
  protocol `Revision`, their digests use distinct runner domains over canonical
  lowercase SHA-256, and artifact bindings are a duplicate-free list of at
  most 128 entries. Equality of a reference proves only that the same reviewed
  record was named; it never proves that record is current or usable.
- `RunOrigin` carries one closed source kind (`interactive`, `automation`,
  `goal`, `trigger`, `schedule`, `recovery`, `graph_child`,
  `background_curation`, `media`, `remote_wakeup`, `batch`, or `evaluation`),
  plus declared causal event IDs and a nested cause when noninteractive. `interactive` requires
  `event_id`, all three typed IDs, causal events and cause to be absent/empty.
  Every noninteractive kind requires an event ID, nonempty causal events and a
  cause whose actor/run match the spec. `automation`, `goal` and `trigger` each require
  their matching typed coordinate and forbid the other two; every other kind
  forbids all three. Origin kind conveys no priority, budget, approval or effect
  authority; those remain independently admitted.
- Credential bindings are non-authorizing requested coordinates. Immediately
  before protected delivery, later use-time code reopens the authoritative
  credential record and verifies exact name, version, recipient, audience,
  subject and current state. A matching sandbox descriptor name alone never
  proves that a credential value may be delivered.
- Canonical encoding is canonical JSON built with
  `automonique_protocol::wire`; fixed top-level keys use the field names in the
  normative table below and platform bytes use lowercase hexadecimal. A public
  `MAX_RUN_SPEC_BYTES` is exactly equal to protocol `MAX_FRAME_BYTES`. Decode
  refuses a larger byte slice before parsing, and encode refuses if the final
  canonical byte vector is larger than that same ceiling.
  The decoder rejects missing, unknown and duplicate fields, unsupported
  versions, noncanonical encoded ordering or spelling, widened bounds and
  trailing bytes. Re-encoding a decoded value is byte-identical.
- Every optional key is present and uses JSON `null` when absent. Declaration
  order is preserved for argv, environment, fallbacks, capabilities, context
  components, origin causal events, artifact bindings and credential bindings;
  collections reject duplicates only where their domain type defines an
  identity. Context components have no such identity and are preserved exactly,
  including repeated equal values. Object keys follow `wire` canonical
  lexicographic ordering. Platform bytes are lowercase
  even-length hexadecimal. Unsigned quantities and revisions are canonical
  decimal strings matching `0|[1-9][0-9]{0,19}` and must parse to `u64`;
  revisions additionally refuse zero. This avoids the protocol wire's `i64`
  integer ceiling.
- The recursive grammar appendix below closes both runner-owned and existing
  protocol-owned value shapes. The public v1 codec must implement that appendix
  exactly. Scheduler bindings are exactly
  `{id,revision,decision_digest}`; artifact bindings are exactly
  `{id,revision,grant_digest}`. Origin is exactly
  `{source,event_id,automation_id,goal_id,trigger_id,causal_events,cause}`;
  optional IDs use `null`. `cause` is either `null` for interactive or the
  closed projection of `NestedCause`
  `{actor,run_id,causation_id,parent_id}` and `parent_id` uses `null` when
  absent. Causal events preserve declared order and reject duplicates.
- The whole-spec SHA-256 digest is domain-separated and covers every canonical
  byte: `SHA256(b"automonique.run-spec/v1\0" || canonical_json)`, rendered as
  lowercase `sha256:` plus 64 hexadecimal digits. Equal specs have equal
  digests; a one-field mutation changes it.

## Canonical JSON grammar appendix

This appendix is normative. It fixes the complete recursive JSON projection of
the current `RunSpec`; an implementation may not add a convenience key, omit a
derived-empty key, encode a Rust discriminant, or substitute a debug spelling.
The notation is:

- `S` is a JSON string accepted by the owning domain constructor.
- `H` is a JSON string containing an even number of lowercase hexadecimal
  digits. It losslessly represents the bytes of a Unix platform value. Empty
  bytes are `""`; a JSON text string is never used in place of `H`.
- `U` is a JSON string matching `0|[1-9][0-9]{0,19}` whose value parses as a
  `u64` and satisfies the owning field's bound.
- `R` is a `U` that also refuses zero and constructs protocol `Revision`.
- `NZ` is a `U` that also refuses zero and constructs `NonZeroU64`. It is not a
  protocol `Revision` and is used for the independently versioned credential
  binding.
- `D` is a canonical SHA-256 JSON string: `sha256:` followed by exactly 64
  lowercase hexadecimal digits. It is used for provider-binary and every
  runner-owned domain digest in this schema.
- `PD` is one canonical protocol `Digest` JSON string
  `<algorithm>:<lowercase-hex>`. The algorithm is `sha256`, `sha512`, or
  `blake3`, and the body has the exact length required by that algorithm. It is
  used only by the nested sandbox domains that already accept protocol
  `Digest`.
- `null|T` means the key is mandatory and its value is either JSON `null` or
  `T`. There are no omitted optional keys.

All object key sets below are exact. On the wire, object keys occur in ascending
UTF-8 byte order, as produced by `automonique_protocol::wire`; the presentation
below uses that order. Arrays retain their declared order unless a more specific
rule below fixes their order. JSON integer, boolean and floating-point values
do not represent any RunSpec quantity.

### Whole document and runner-owned values

The whole document is exactly:

```text
{
  "argv_hex": [H],
  "artifact_grants": [ArtifactGrantBinding],
  "attempt_id": S,
  "backend_id": S,
  "context_manifest": ContextManifest,
  "credential_bindings": [CredentialBinding],
  "cwd_token": S,
  "environment_hex": [EnvironmentEntry],
  "event_dialect": "automonique_runner_v1",
  "executable_path_hex": H,
  "execution_plan_digest": D,
  "executor_class": ExecutorClass,
  "extension_set_digest": D,
  "fallback_eligibility": [S],
  "host_id": S,
  "host_lifetime": "attempt" | "session",
  "integration_mode": S,
  "io_reservation": IoReservation,
  "model_routing_digest": D,
  "origin": Origin,
  "persona_digest": D,
  "portability_policy": PortabilityPolicy,
  "profile_digest": D,
  "prompt_delivery": PromptDelivery,
  "provider_binary": BinaryProvenance,
  "remote_attestation_policy": RemoteAttestationPolicy,
  "required_capabilities": [Capability],
  "run_id": S,
  "sandbox_spec": SandboxSpec,
  "scheduler_reservation": SchedulerReservationBinding,
  "schema": "automonique.run-spec/v1",
  "session_binding": null | SessionBinding,
  "skillset_digest": D,
  "toolset_digest": D,
  "work_id": S,
  "workspace": WorkspaceRegistration,
  "workspace_registry_id": S,
  "workspace_reservation": U
}
```

There is no separate `protocol_version` key: the closed schema spelling is the
version coordinate. The executable and argv values are platform bytes, not
UTF-8 paths or arguments. Their existing lexical and aggregate validation runs
after hex decoding. The subordinate runner shapes are:

```text
EnvironmentEntry = {"key_hex": H, "value_hex": H}
IoReservation = {"read_bytes": U, "write_bytes": U}

PromptDelivery = {
  "backend_session": null | S,
  "mode": "stdin" | "protected_reference" | "backend_session",
  "protected_reference": null | S
}

SchedulerReservationBinding = {
  "decision_digest": D,
  "id": S,
  "revision": R
}

ArtifactGrantBinding = {
  "grant_digest": D,
  "id": S,
  "revision": R
}

CredentialBinding = {
  "audiences": [S],
  "name": S,
  "version": NZ
}
```

For prompt mode `stdin`, both coordinate keys are `null`; for
`protected_reference`, only `protected_reference` is a string; for
`backend_session`, only `backend_session` is a string. Scheduler and artifact
binding digests are SHA-256 runner-domain digests. Artifact bindings preserve
declaration order and reject a repeated `id`, including when its revision or
digest differs. Credential bindings preserve declaration order and form the
exact name bijection required by the nested sandbox credential descriptors.
Each `audiences` array is in strictly increasing UTF-8 byte order with no
duplicates, matching the canonical value held by `CredentialAudiences`; a
decoder refuses an input that its constructor would otherwise sort or dedupe.

Origin is exactly:

```text
Origin = {
  "automation_id": null | S,
  "causal_events": [S],
  "cause": null | NestedCause,
  "event_id": null | S,
  "goal_id": null | S,
  "source": OriginSource,
  "trigger_id": null | S
}

NestedCause = {
  "actor": Actor,
  "causation_id": S,
  "parent_id": null | S,
  "run_id": S
}
```

`OriginSource` is exactly `interactive`, `automation`, `goal`, `trigger`,
`schedule`, `recovery`, `graph_child`, `background_curation`, `media`,
`remote_wakeup`, `batch`, or `evaluation`. Interactive origin uses `null` for
all four optional IDs and `cause`, and has an empty causal-event array. Every
noninteractive source requires `event_id`, a nonempty duplicate-free causal
event array and `cause`. `automation`, `goal` and `trigger` require only their
matching typed coordinate; the other noninteractive sources require all three
coordinate keys to be `null`.

Executor and portability policy are:

```text
ExecutorClass = {
  "kind": "local" | "container" | "ssh" | "batch" | "cluster" |
          "micro_vm" | "remote",
  "remote_coordinate": null | RemoteCoordinate
}

RemoteCoordinate = {"resource_id": S, "vendor": S}

PortabilityPolicy = {
  "artifact_transfer": null | ArtifactTransfer,
  "kind": "pinned" | "portable",
  "workspace_transfer": null | WorkspaceTransfer
}
```

`remote` requires a coordinate and every other executor kind requires `null`.
`pinned` requires both transfer keys to be `null`; `portable` requires both to
be strings. `WorkspaceTransfer` is exactly `shared_mount`,
`synchronized_over_attested_channel`, or `content_addressed_bundle`.
`ArtifactTransfer` is exactly `digest_verified_pull` or
`digest_verified_push`. `RemoteAttestationPolicy` is exactly `not_required`,
`signed`, or `mutually_authenticated`.

### Provider and workspace values

```text
BinaryProvenance = {
  "digest": D,
  "schema_digest": null | D,
  "version": S
}

WorkspaceRegistration = {
  "base_revision": R,
  "canonical_source": S,
  "isolation": WorkspaceIsolation,
  "snapshot": S,
  "tenant": S,
  "token": S
}

SessionBinding = {
  "backend": S,
  "provider_account": S,
  "provider_namespace": S,
  "session": S,
  "tenant": S
}

Capability = {"group": CapabilityGroup, "name": S}
```

Both binary digest fields, when present, require SHA-256. `WorkspaceIsolation`
is exactly `read_only_snapshot`, `attempt_copy`, or `overlay`.
`CapabilityGroup` is exactly `sessions`, `turns`, `events`, `approvals`,
`tools`, `telemetry`, or `lifecycle`.

### Context manifest

```text
ContextManifest = {
  "policy": [PolicyComponent],
  "policy_revision": R,
  "supplied": [SuppliedComponent],
  "token_budget": U
}

ComponentCaps = {"byte_cap": U, "token_cap": U}

PolicyComponent = {
  "caps": ComponentCaps,
  "digest": S,
  "redaction": RedactionOutcome,
  "revision": R,
  "source": S
}

SuppliedComponent = {
  "caps": ComponentCaps,
  "class": SuppliedClass,
  "digest": S,
  "redaction": RedactionOutcome,
  "source": S,
  "trust": SuppliedTrust
}
```

`RedactionOutcome` is exactly `clean` or `redacted`. `SuppliedClass` is exactly
`workspace_rules`, `skills`, `memory`, `tools`, `mcp`, `attachments`, or
`conversation`. `SuppliedTrust` is exactly `untrusted`, `compatibility`, or
`actor_supplied`; `policy` is refused rather than passed to the constructor that
would lower it. Policy class and trust are not encoded because their distinct
type fixes them as `system_policy` and `policy`.

Both component arrays preserve every element in declaration order. The codec
adds no component-count bound beyond `wire::MAX_JSON_ENTRIES` and the whole
document ceiling, and adds no guessed duplicate identity: equal source,
digest, class or complete component values may be intentionally repeated and
must round-trip as repeated values. Existing per-string bounds, nonzero
component caps and the manifest's aggregate token-budget check still apply.
This conservative rule may be tightened only after the context domain defines
a stable component identity; a decoder must not currently sort or dedupe these
arrays.

### Sandbox specification

```text
SandboxSpec = {
  "actor": Actor,
  "allowlists": [AllowlistEntry],
  "approval_revision": R,
  "base_revision": R,
  "budgets": Budgets,
  "credentials": [CredentialDescriptor],
  "nested_isolation": NestedIsolation,
  "path_grants": [PathGrant],
  "policy_digest": PD,
  "profile": SandboxProfile,
  "prohibited_capabilities": [S],
  "provider_account": S,
  "provider_control_egress": NetworkAccess,
  "required_features": [RequiredFeature],
  "tool_workload_egress": NetworkAccess,
  "workspace_context": PD
}

Actor = {"id": S, "tenant": S}

SandboxProfile = {
  "filesystem": FilesystemAccess,
  "id": S,
  "tool_network": NetworkAccess,
  "version": U
}

PathGrant = {"access": PathAccess, "path": S}
AllowlistEntry = {"class": AllowlistClass, "name": S}
CredentialDescriptor = {"name": S, "recipient": ProcessClass}

Budgets = {
  "artifact_bytes": U,
  "cgroup_cpu_millicores": U,
  "cgroup_memory_bytes": U,
  "rlimit_descriptors": U,
  "rlimit_processes": U,
  "spool_bytes": U,
  "temporary_storage_bytes": U,
  "timeout_millis": U
}

RequiredFeature = {
  "accepted_implementations": [PD],
  "name": S
}

NestedIsolation = {
  "extensions": IsolationRequirement,
  "nested_tools": IsolationRequirement
}
```

The exact sandbox spellings are:

- `FilesystemAccess`: `none`, `read_only_snapshot`, `isolated_writable`, or
  `writable_with_grants`.
- `NetworkAccess`: `denied`, `brokered_named`, or `brokered_any`.
- `PathAccess`: `read_only` or `read_write`.
- `AllowlistClass`: `executable`, `interpreter`, `tool`, `mcp_server`, or
  `companion`.
- `ProcessClass`: `provider_adapter`, `tool_process`, `mcp_server`, `companion`,
  or `extension_worker`.
- `IsolationRequirement`: `host_boundary`, `separate_child_boundary`, or
  `stronger_isolation`.

Sandbox arrays preserve declaration order. Existing domain identities remain
authoritative: path grants reject a repeated path; allowlists reject a repeated
class/name pair; credentials reject a repeated name; required features reject
a repeated feature name; and prohibited capabilities reject a repeated value.
The accepted-implementation array inside one required feature has no separate
identity or duplicate prohibition in the current protocol type, so it is
preserved exactly and is bounded only by its existing domain and wire limits.
Sandbox tagged digests use `PD` and retain every algorithm accepted by protocol
`Digest`; they must not be silently narrowed to SHA-256. In contrast, context
component `digest`, workspace `snapshot` and workspace `canonical_source` are
bounded `S` values owned by their existing constructors, not `D` or `PD`, and a
codec must not infer digest syntax for them.

### Array ordering, bounds and identities

Every array is first subject to `wire::MAX_JSON_ENTRIES` and the whole-document
ceiling. More specific constructors then apply the following exact rules; no
other duplicate identity is inferred:

| Array | Order and domain rule |
|---|---|
| `argv_hex` | Preserve declaration order; runner argument count/item/aggregate bounds apply; duplicate byte strings are allowed. |
| `environment_hex` | Preserve declaration order; runner count/item/aggregate bounds apply; duplicate decoded keys refuse even when values differ. |
| `fallback_eligibility` | Preserve declaration order; at most 16; exact mode duplicates and the selected integration mode refuse. |
| `required_capabilities` | Preserve declaration order; at most protocol `MAX_CAPABILITIES`; duplicate complete `(group,name)` values refuse. The same name in different groups is distinct. |
| `context_manifest.policy` | Preserve declaration order; at most `wire::MAX_JSON_ENTRIES`; duplicates are allowed because the protocol defines no stable component identity. |
| `context_manifest.supplied` | Preserve declaration order; at most `wire::MAX_JSON_ENTRIES`; duplicates are allowed for the same reason. |
| `origin.causal_events` | Preserve declaration order; noninteractive values contain 1 to 64 entries; repeated event IDs refuse. Interactive is exactly empty. |
| `artifact_grants` | Preserve declaration order; at most 128; repeated grant IDs refuse regardless of revision or digest. |
| `credential_bindings` | Preserve declaration order; binding names form the exact bijection with sandbox credential-descriptor names, so duplicate or missing names refuse. |
| `credential_bindings[*].audiences` | Canonical value is strictly increasing UTF-8 byte order with no duplicates. Decode refuses rather than relying on `CredentialAudiences` to sort or dedupe. |
| `sandbox_spec.path_grants` | Preserve declaration order; at most protocol `MAX_SANDBOX_ENTRIES`; repeated paths refuse regardless of access. |
| `sandbox_spec.allowlists` | Preserve declaration order; at most protocol `MAX_SANDBOX_ENTRIES`; repeated complete `(class,name)` entries refuse. The same name in different classes is distinct. |
| `sandbox_spec.credentials` | Preserve declaration order; at most protocol `MAX_SANDBOX_ENTRIES`; repeated names refuse regardless of recipient. |
| `sandbox_spec.required_features` | Preserve declaration order; nonempty and at most protocol `MAX_SANDBOX_ENTRIES`; repeated feature names refuse. |
| `required_features[*].accepted_implementations` | Preserve declaration order; nonempty and at most protocol `MAX_SANDBOX_ENTRIES`; duplicate `PD` values are allowed by the current constructor. |
| `sandbox_spec.prohibited_capabilities` | Preserve declaration order; at most protocol `MAX_SANDBOX_ENTRIES`; repeated values refuse. |

Constructor-specific normalization is not a wire permission. In particular,
the decoder checks audience order/uniqueness before calling the normalizing
constructor. Conversely, the decoder may not invent a uniqueness check for an
array whose constructor preserves duplicates.

### Decoder canonicalization and refusal

The decoder performs these steps in order:

1. Refuse a document over the public whole-document ceiling before parsing.
2. Parse with `automonique_protocol::wire::parse_canonical`, thereby refusing
   malformed UTF-8/JSON, duplicate object keys, noncanonical escaping or key
   order, whitespace, trailing bytes, excessive nesting and wire collection or
   string bounds.
3. At every object depth require exactly the key set above. Refuse a missing,
   unknown or wrong-type key and refuse an omitted optional key.
4. Parse `H`, `U`, `R`, `NZ`, `D`, `PD` and every enum by exact spelling before
   invoking domain constructors. Refuse uppercase hex, odd hex, leading-zero
   decimal, overflow, a zero `R` or `NZ`, wrong digest
   algorithm/length or normalized enum aliases.
5. Refuse any array whose domain-defined duplicate or ordering rule is
   violated. Do not sort, dedupe, case-fold or otherwise normalize an input.
6. Reconstruct protocol-owned values through their public constructors,
   reconstruct the complete value through `RunSpec::new`, then encode it again.
   The re-encoded bytes must equal the input byte for byte.

An encoder recursively projects only the fields above and then uses
`JsonValue::to_canonical_bytes`; it checks the exact `MAX_FRAME_BYTES` ceiling
again on the completed byte vector. Canonical parse plus exact typed
reconstruction plus whole-value re-encoding is required; any one of those
checks alone is insufficient. Neither successful decode nor a stable digest is
an authorization or evidence that any referenced service record is current.

## Normative requirement map

Each row fixes the public accessor or nested accessor and canonical JSON key.
An implementation test mirrors this table, but the contract—not that test—is
the authority. Every row needs a positive accessor/key assertion plus a
one-field mutation that changes canonical bytes and the digest or produces a
typed refusal. Acceptance requires 20/20 rows and zero duplicated authority.

| # | Product requirement | RunSpec field / canonical owner | JSON key(s) |
|---:|---|---|---|
| 1 | protocol, work, run, attempt, host, lifetime, backend | protocol version; `host::{WorkId, AttemptId, HostId, HostLifetime}`; `tools::RunId`; sandbox `ExecutionBackendId` | `schema`, `work_id`, `run_id`, `attempt_id`, `host_id`, `host_lifetime`, `backend_id` |
| 2 | executable identity and exact argv | provider `BinaryProvenance`, changed to require canonical lowercase `sha256:` digests and full read-only projection; runner lossless executable path and argv bytes | `provider_binary`, `executable_path_hex`, `argv_hex` |
| 3 | prompt delivery | runner closed `PromptDeliveryPlan::{Stdin, ProtectedReference, BackendSession}` with no payload | `prompt_delivery` |
| 4 | workspace registry/base/cwd/access | runner `WorkspaceRegistryId`; protocol `WorkspaceRegistration` with complete projection; runner cwd token; registration isolation is the read/write policy | `workspace_registry_id`, `workspace`, `cwd_token` |
| 5 | environment allowlist values | runner ordered unique lossless byte map | `environment_hex` |
| 6 | timeout and general resource limits | authoritative `SandboxSpec::budgets()` plus checked runner-supported nested timeout/spool envelopes | `sandbox_spec` |
| 7 | sandbox profile/policy/features/grants | authoritative protocol `SandboxSpec` canonical projection | `sandbox_spec` |
| 8 | distinct provider/tool egress | authoritative `SandboxSpec` provider-control and tool-workload egress | `sandbox_spec` |
| 9 | complete quota reservations | `SandboxSpec::budgets()` for existing axes plus runner `IoReservation` and `WorkspaceReservation` | `sandbox_spec`, `io_reservation`, `workspace_reservation` |
| 10 | nested isolation and implementation digests | authoritative `SandboxSpec` nested isolation and required-feature implementations | `sandbox_spec` |
| 11 | optional resumable session | provider `SessionBinding` with complete read-only canonical projection | `session_binding` |
| 12 | native mode and eligible fallbacks | runner domain-typed bounded provider-declared `IntegrationMode` and duplicate-free ordered `FallbackEligibility`; the selected mode cannot occur in its fallback set | `integration_mode`, `fallback_eligibility` |
| 13 | required provider and prohibited OS capabilities | runner bounded duplicate-free `RequiredCapabilities` over provider `Capability`, later checked against pinned observed mode selection; independent authoritative `SandboxSpec` OS prohibitions; no cross-namespace conversion | `required_capabilities`, `sandbox_spec` |
| 14 | context/profile/model/tool/skill/extension revisions | protocol `ContextManifest`; runner domain-tagged profile/model-routing/toolset/skill-set/extension-set digests | `context_manifest`, `profile_digest`, `model_routing_digest`, `toolset_digest`, `skillset_digest`, `extension_set_digest` |
| 15 | durable origin and causes | runner closed source-kind `RunOrigin` with mandatory noninteractive origin-event ID; protocol `automation::DurableId` and `tools::NestedCause` for typed refinements and causation | `origin` |
| 16 | executor/portability/remote-attestation policy | protocol plan-time `models::ExecutorClass`; runner `PortabilityPolicy` and `RemoteAttestationPolicy` (never observed lifecycle evidence) | `executor_class`, `portability_policy`, `remote_attestation_policy` |
| 17 | provider executable/protocol digest | provider `BinaryProvenance` | `provider_binary` |
| 18 | policy/persona/plan/scheduler reservation | authoritative sandbox policy revision/digest; runner persona and execution-plan digests; non-authorizing `SchedulerReservationBinding` issued by the scheduler | `sandbox_spec`, `persona_digest`, `execution_plan_digest`, `scheduler_reservation` |
| 19 | tenant/actor/artifacts/credential descriptors | authoritative `SandboxSpec` actor/tenant/credential names and recipients; non-authorizing `ArtifactGrantBinding` references issued by the artifact service; credential version/audience binding, with no value field | `sandbox_spec`, `artifact_grants`, `credential_bindings` |
| 20 | output dialect and approval-policy binding | runner closed `RunnerEventDialect`; authoritative `SandboxSpec` policy digest and approval revision. Tool invocation cadence remains on each `ToolDescriptor` and is not duplicated here | `event_dialect`, `sandbox_spec` |

The codec contract includes one fixed full-spec golden JSON byte vector as a
literal byte string with no trailing line feed, plus a literal expected
domain-separated SHA-256 digest calculated outside the product implementation.
The golden expectation may not call the production encoder, canonical JSON
builder, digest helper or fixture constructor to derive either literal.

## Verification contract

| Check | Expected |
|---|---|
| Encapsulation | rustdoc compile-fail controls refuse private construction and mutation and mixed work/attempt/host identities; paired public getters compile and preserve the typed values |
| Complete valid spec | one full value covers all 20 requirement groups and names the canonical nested contracts used for each group |
| Bounds | exact `MAX_FRAME_BYTES` pre-parse and post-encode positives plus one-over negatives cover whole-document bytes; exact-limit positives and one-over negatives cover identifier/digest, argument item/count/aggregate, environment key/value/count/aggregate, protected reference, duration and every resource quota |
| Cross-field invariants | one-field negatives cover every source-backed shared semantic named above; tests explicitly prove provider capabilities are not conflated with OS prohibitions and tool approval cadence is not inferred from a sandbox revision |
| Closed codec | a valid round trip is byte-identical; missing, unknown, duplicate, reordered encoded bytes, wrong-type, unsupported-version, NUL, traversal, noncanonical and oversized inputs refuse with typed categories |
| Digest | repeated construction is stable and mutation of every top-level field family changes the whole-spec digest |
| Digest spelling | executable and optional protocol digests accept only canonical lowercase `sha256:` plus 64 hexadecimal digits; wrong algorithm, length, character or case refuses |
| Protected data | prompt bytes and credential secret values have no structural input route. Protected-reference and backend-session fields encode only their bounded routing coordinates. Canonical bytes intentionally serialize argv, environment and reference metadata, so the schema makes no secrecy claim for those caller-controlled values; `Debug`, display and errors must still use their documented redaction behavior and never add a secret-value field |
| Golden independence | a literal full-spec JSON byte string has no trailing LF and a literal expected digest is calculated outside the product implementation; the expected bytes/digest do not invoke production encoding, canonicalization, hashing or fixture helpers |
| Fail-closed execution | a valid complete spec still returns the existing typed containment refusal and creates no process, network connection, spool or external effect |
| Quality | runner tests, workspace tests, strict Clippy, formatting and diff checks pass offline and locked; lockfile inspection shows only the runner-to-existing-protocol edge and no registry version drift |

Required commands:

```text
cargo test -p automonique-runner --all-targets --locked --offline
cargo test -p automonique-runner --doc --locked --offline
cargo clippy -p automonique-runner --all-targets --all-features --locked --offline -- -D warnings
cargo fmt --all -- --check
cargo test --workspace --all-targets --locked --offline
git diff --check
```

The admitted-base runner baseline is 22 passing tests, zero failures, with
strict Clippy and formatting green. The implementation report records actual
accepted and refused case counts, error categories, the 20-row coverage result,
the exact lockfile delta and reviewer count. Missing measurements remain
`null` with a reason.

## Compatibility and non-goals

This is admission-time schema work only. It performs no path opening,
descriptor ownership or mode check, symlink or TOCTOU proof, protected payload
delivery, process spawn, provider invocation, network I/O, OS sandbox
installation, host attestation, artifact promotion or external effect. Those
belong to later R2 work. Existing fail-closed runner behavior is preserved.

Only the checked-in product requirements, the structural migration map, public
dependency contracts and synthetic fixtures may inform the implementation.
`rust/Cargo.lock` is authorized solely to add the existing local
`automonique-protocol` dependency to `automonique-runner`; no registry package
or version may change. Protocol edits are limited to lossless read-only
projection/canonical-codec support required by the table and paired tests; they
may not add runtime behavior, mutable authority or a second schema.

## Stop conditions

Stop on any protocol edit beyond the narrow read-only projection/codec work
above, any new or updated registry dependency, a second RunSpec, a duplicated protocol type that already
exists or represent a missing domain concept as an untagged string, put
prompt bytes or a credential value in the document, expose a bypass constructor
or mutable field, accept lossy platform strings, leave a required group
unmapped, treat schema validation as use-time containment evidence, weaken the
runner's current execution refusal, access the network or private archive,
cross the licence boundary, touch a path outside the implementation allowlist,
or encounter an unrelated dirty-path overlap.
