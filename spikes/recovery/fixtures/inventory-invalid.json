{
  "schema": "automonique.recovery.restore-dependencies.v1",
  "item": "R0-09",
  "dependencies": [
    {
      "id": "control-database",
      "order": 4,
      "kind": "relational_cluster",
      "source": "backup_artifact",
      "verified_by": "sqlite_integrity_check",
      "owner_class": "control-plane",
      "note": "synthetic fixture entry; 'kind' is outside the closed vocabulary on purpose, so the parser must refuse the whole inventory rather than keep the rest"
    }
  ]
}
